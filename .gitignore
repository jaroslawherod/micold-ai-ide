# Spec Kit local feature pointer (per-workspace, not shared)
.specify/feature.json

# Rust
/target/
# Shared across the main checkout and every worktree (see scripts/build-lock.sh)
/target-shared/
**/*.rs.bk
*.pdb

# Editor / OS
.idea/
.vscode/
.DS_Store
Thumbs.db
*.swp
