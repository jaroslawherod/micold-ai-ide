# Autopilot ledger — 006-real-terminal-emulator BUG-007

Maintained by the `speckit-autopilot` skill. It is the record of what this flow owns and how far it
has got. `resume` finds this file by its **Worktree branch** line and reads it. Keep it true.

- **Input**: bug: a single left click in a terminal pane leaves a one-cell selection highlight until the next click
- **Kind**: bug
- **Worktree branch**: fix/terminal-click-highlight
- **Started**: 2026-09-14
- **Phase**: 4-milestones
- **Next step**: M1 through the milestone loop — `speckit-implement` (T074 Red, T075 Green, T076 visual pass + guide), gate, reviews A+B, PR

## Pull requests

| PR | Purpose | Status | Merge SHA |
|---|---|---|---|

## Milestones

| ID | Tasks | Deliverable | PR | Status |
|---|---|---|---|---|
| M1 | T074–T076 | A single click in a terminal pane selects nothing: no highlight, clipboard untouched | — | in progress |

## Decisions

| # | Phase | Question | Answer | By | Evidence |
|---|---|---|---|---|---|
| D1 | bug | Owning spec? | 006-real-terminal-emulator — Status Closed, no autopilot ledger | agent-resolved | `specs/006-real-terminal-emulator/spec.md` Status line; FR-013/FR-013a |
| D2 | bug | Does a drag that leaves the pressed cell and comes back still select that one cell? | Yes — only a selection whose moving end never left the pressed cell is empty, so single-character selection stays reachable | agent-resolved | FR-013a ("dragging selects"); `selection.rs` inclusive-bounds contract |
| D3 | bug | Fix size | Bug path (3 tasks, no behaviour the spec did not intend) | agent-resolved | Clarifications "dragging selects"; `evidence/bugfix-pass-2026-09-13.md` step 2, BUG-004 and BUG-006 use "click once to clear the selection" |
| D4 | bug | Bug-rubric review, round 1 F1: fixing `Selection` alone leaves a tap (press+release in one event batch) copying the *previous* selection from the pane's pre-batch view. Fix it in this flow? | Yes. The release publishes a message and the shell copies from the current `app.selection`. This is the same FR-013e clause ("nothing is copied"), not new behaviour | agent-resolved | iced_winit 0.14 `lib.rs` batches events into one `user_interface.update`; `terminal_pane.rs` left-release arm |
| D5 | bug | Round 2 F1/F2 and round 3 F1: where is "a drag" decided, and should the release reuse `TerminalCopyRequested`? | In the pane, in viewport cells: no `TerminalSelectUpdate` until the pointer leaves the pressed cell. A `Char` `Selection` is empty until its first update, because `LineId` anchors drift at the live bottom. A new `TerminalSelectionReleased` is used, because `on_copy_requested` also closes the context menu | agent-resolved | `main.rs` `row_line_id`; `shell/clipboard.rs` `on_copy_requested` |
| D6 | bug | Bug-rubric review outcome | Clean after 3 rounds (round 3 minor only, fixed: spec Status parenthetical, single drag judge) | agent-resolved | reviewer rounds 1–3 |

## Declined review findings

| Milestone | Review | Finding | Why declined |
|---|---|---|---|

## Open escalation

None.

## Follow-ups not done

None yet.
