# Autopilot ledger — 006-real-terminal-emulator BUG-007

Maintained by the `speckit-autopilot` skill. It is the record of what this flow owns and how far it
has got. `resume` finds this file by its **Worktree branch** line and reads it. Keep it true.

- **Input**: bug: check the screen shot the claude cli is showing back on black text. For human is not readable [Image #1]
- **Kind**: bug
- **Worktree branch**: fix/black-on-black
- **Started**: 2026-09-14
- **Phase**: 0-bug
- **Next step**: M1 — wait for 030's Windows install smoke (step 9a) to be fixed on main; then rebase #357 onto origin/main, `mise run gate`, push, merge on green, hand off

## Pull requests

| PR | Purpose | Status | Merge SHA |
|---|---|---|---|
| #357 | M1: BUG-007 fix | open | — |

## Milestones

| ID | Tasks | Deliverable | PR | Status |
|---|---|---|---|---|
| M1 | T074–T080 | A program asking a dark pane for its background (`OSC 11`) is told dark; `claude` draws legible text | #357 | in review |

## Decisions

| # | Phase | Question | Answer | By | Evidence |
|---|---|---|---|---|---|
| D1 | bug | Which spec owns the defect? | 006 (FR-003, Closed); 010 protocol §8 is the seam, amended the way 006 BUG-003 amended §5 | agent-resolved | `specs/010-daemon-session-persistence/contracts/protocol.md#5` ("the third added by BUG-003 of `006-real-terminal-emulator`") |
| D2 | bug | Answer queries daemon-side from a reported scheme, or round-trip them to the client? | Daemon-side, from the last scheme a client reported | agent-resolved | `specs/010-daemon-session-persistence/contracts/protocol.md#8` (replies stay daemon-side) |
| D3 | bug | Review round 1 F1: send DEC mode 2031 theme-change notifications (`CSI ? 997 n`) so a running `claude` repaints on a switch? | No — out of this fix; recorded as a spec Edge Case and a follow-up. The reported failure (dark pane, `claude` started dark) is fixed by FR-003a alone; 2031 is a VT capability 006 never listed and most standalone terminals lack (SC-002 parity) | agent-resolved | `specs/006-real-terminal-emulator/spec.md#edge-cases` (BUG-007 entry); reviewer's pty probe of `claude` 2.1.270 |
| D4 | bug | Fix size: one milestone or Phase 1? | One milestone — 7 tasks, FR-003a is intended by FR-003 + SC-002 (review round 2 CLEAN) | agent-resolved | `specs/006-real-terminal-emulator/tasks.md#phase-14` |

## Declined review findings

| Milestone | Review | Finding | Why declined |
|---|---|---|---|
| M1 | B | F2 MINOR: `#![cfg(unix)]` on all of `terminal_color_scheme.rs` compiles U11 out on Windows | Windows CI runs only `windows_process_tree`, so it changes nothing today, and per-test `cfg` would leave the shared helpers and imports unused on Windows (a `-D warnings` hazard) |
| M1 | B | F3 MINOR: `reported_scheme` is cleared in both `on_disconnected` and `on_connected`; the first is untested | Kept deliberately: the disconnect clear keeps `App` truthful while no connection exists; harmless |

## Open escalation

- 2026-09-16, M1 #357, blocked by work outside my flow: `build + test (windows-latest)` fails in 030's
  `scripts/windows-install-smoke.sh` step 9a (`…pipe… is up, but no micold-daemon.exe process was found
  behind it`) on every run — 3 runs of this branch, then a control run (35136830136) with `crates/`
  identical to origin/main plus failure diagnostics, which failed the same way. Diagnostics: the pid
  record names a daemon that is no longer running, no micold-daemon.exe exists, the client is alive,
  and Windows logged no crash event. Temporary diagnostic/control commits were dropped from the branch.

## Follow-ups not done

- The daemon answers `OSC 4` for indices 0–15 from xterm's system colours; the client draws them from
  `STANDARD_ANSI16`. Reported under BUG-007's root cause; not observed to break anything.
- DEC private mode 2031 (theme-change notification, `CSI ? 997 ; 1|2 n`) is not implemented, so a
  `claude` started before a theme switch keeps its old palette until restarted.
