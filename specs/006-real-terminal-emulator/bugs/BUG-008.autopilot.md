# Autopilot ledger — 006-real-terminal-emulator BUG-008

Maintained by the `speckit-autopilot` skill. It is the record of what this flow owns and how far it
has got. `resume` finds this file by its **Worktree branch** line and reads it. Keep it true.

- **Input**: bug: a single left click in a terminal pane leaves a one-cell selection highlight until the next click
- **Kind**: bug
- **Worktree branch**: fix/terminal-click-highlight
- **Started**: 2026-09-14
- **Phase**: done
- **Next step**: none — #345 merges on green with `--rebase` (recorded before the merge; GitHub is the source of truth for its state)

## Pull requests

| PR | Purpose | Status | Merge SHA |
|---|---|---|---|
| #345 | M1: BUG-008 record, spec patch, regression tests, fix | merge on green (rebase) | — |

## Milestones

| ID | Tasks | Deliverable | PR | Status |
|---|---|---|---|---|
| M1 | T081–T083 | A single click in a terminal pane selects nothing: no highlight, clipboard untouched | #345 | done |

## Decisions

| # | Phase | Question | Answer | By | Evidence |
|---|---|---|---|---|---|
| D1 | bug | Owning spec? | 006-real-terminal-emulator — Status Closed, no autopilot ledger | agent-resolved | `specs/006-real-terminal-emulator/spec.md` Status line; FR-013/FR-013a |
| D2 | bug | Does a drag that leaves the pressed cell and comes back still select that one cell? | Yes — only a selection whose moving end never left the pressed cell is empty, so single-character selection stays reachable | agent-resolved | FR-013a ("dragging selects"); `selection.rs` inclusive-bounds contract |
| D3 | bug | Fix size | Bug path (3 tasks, no behaviour the spec did not intend) | agent-resolved | Clarifications "dragging selects"; `evidence/bugfix-pass-2026-09-13.md` step 2, BUG-004 and BUG-006 use "click once to clear the selection" |
| D4 | bug | Bug-rubric review, round 1 F1: fixing `Selection` alone leaves a tap (press+release in one event batch) copying the *previous* selection from the pane's pre-batch view. Fix it in this flow? | Yes. The release publishes a message and the shell copies from the current `app.selection`. This is the same FR-013e clause ("nothing is copied"), not new behaviour | agent-resolved | iced_winit 0.14 `lib.rs` batches events into one `user_interface.update`; `terminal_pane.rs` left-release arm |
| D5 | bug | Round 2 F1/F2 and round 3 F1: where is "a drag" decided, and should the release reuse `TerminalCopyRequested`? | In the pane, in viewport cells: no `TerminalSelectUpdate` until the pointer leaves the pressed cell. A `Char` `Selection` is empty until its first update, because `LineId` anchors drift at the live bottom. A new `TerminalSelectionReleased` is used, because `on_copy_requested` also closes the context menu | agent-resolved | `main.rs` `row_line_id`; `shell/clipboard.rs` `on_copy_requested` |
| D6 | bug | Bug-rubric review outcome | Clean after 3 rounds (round 3 minor only, fixed: spec Status parenthetical, single drag judge) | agent-resolved | reviewer rounds 1–3 |
| D7 | 4 | TDD cadence for a feature with no `tdd/` directory | A BUG-008-scoped `tdd/test-list.md`; the failing tests land as one Red commit and the fix as one Green commit (the repo convention), with deliberate mutants for the three tests that pass on `origin/main` | agent-resolved | tasks.md Phase 15 purpose; `tdd/cycle-log.md` |
| D8 | 4 | Review A (round 1) MINOR F1/F2: the pressed-cell check treats a drag as a click when the view scrolls under a held button, or when the pointer leaves the pane past a pressed edge cell. Fix them? | Yes. Both are the FR-013e boundary ("released before the pointer has entered any other cell"), and each fix is one line. Added as U25/U26 | agent-resolved | `terminal_pane.rs` wheel `ScrollLocally` arm; `grid_at` clamps to the origin |
| D9 | 4 | Round 2: Review A MAJOR F1 and Review B MINOR F1 say a press in the focus gutter becomes a drag on the first jitter. Review A MINOR F2 says a wheel turn that cannot scroll still ends the click. Fix them, and how? | Yes, both are FR-013e. Only a position outside the pane (`bounds`) leaves the pressed cell, so the gutter belongs to its edge cell. This is Review B's suggestion; Review A's unclamped signed cell would instead make gutter-to-edge-cell motion a drag. The pressed cell is cleared only when the scroll would change the offset, using the same clamp as `main.rs`. Added as U27/U28. Review B F2/F3 (the task, plan and contract text for U25–U28; the baseline commit) were fixed in the docs | agent-resolved | `terminal_pane.rs` press arm `cursor.is_over(bounds)`; `main.rs` `TerminalScrolled` clamp |
| D10 | 4 | Round 3: Review A CLEAN (2 MINOR, "Fix: none needed"), Review B CLEAN (2 MINOR). Act on them? | Review B F1: the pane comment is narrowed to the top and left gutters. Past the last column or row, `grid_at` does not clamp, so that is a cell of its own and moving into the last real column is a move; behaviour is unchanged. Review B F2: the evidence names the "after" commit and says why U25–U28 do not touch its checks. Review A F1/F2 are declined (below). Only a comment changed in code, so no fourth review round | agent-resolved | reviewer round 3 |

## Declined review findings

| Milestone | Review | Finding | Why declined |
|---|---|---|---|
| M1 | A, round 3 | F1 MINOR: the wheel check uses the offset from the pane's pre-batch view, so an earlier wheel turn or keystroke in the same batch can make it wrong | The reviewer found it needs three or more input events in one frame. The common failure direction (not clearing) is the safe side, a click that selects nothing. The suggested per-gesture scroll sum adds state for a case no user reaches |
| M1 | A, round 3 | F2 MINOR: pressing in column 0 and slipping more than 3 px past the pane edge counts as a drag | Intended and decided in D8 (U26): a pointer that has left the pane has entered no cell of it, and treating it as still in the pressed cell was the round-1 finding |

## Open escalation

None.

## Follow-ups not done

- On this machine, `crates/micold-daemon/tests/exclusivity.rs`
  `one_conversation_one_session::a_second_open_of_a_held_pi_conversation_starts_nothing` fails
  (`left: 0 right: 1`). It fails identically on `origin/main` `9780f1ac`, where CI is green, so it is
  environmental and not this flow's code. It was not investigated further.
