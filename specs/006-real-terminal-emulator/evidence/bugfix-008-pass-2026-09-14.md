# 006 T083: visual pass for BUG-008

**Date**: 2026-09-14
**Run by**: an agent, not a person at a display. Xvfb `:94` at 1600×1400 with Mesa lavapipe
(software Vulkan), driven with `xdotool` and captured with `import`, following the repo's
`visual-pass` skill. The clipboard was read and set with a small GTK script on `:94`.
**Builds**: "before" is `origin/main` `226d3a8b`, and "after" is this branch's T082 fix (`9511fa03`
once rebased onto `fadb8e9f`). The later pane changes U25–U28 (a wheel scroll or leaving the pane
while held, a press in the focus gutter) do not touch any path checked here, which are clicks and
drags inside cells with no scrolling. Their pane unit tests hold them. Each pair of
`micold-ai-ide` and `micold-daemon` came from one locked invocation. `Compiling micold-core`,
`micold-client` and `micold-daemon` were all printed, and the binaries were copied to a private pin
directory inside the lock. The after client contains `on_selection_released` and the before client
does not. The sandbox daemon log shows `client attached to daemon`, with no `refusing client`.
**Isolation**: private `XDG_RUNTIME_DIR=/tmp/vptc`, plus a scratch `XDG_DATA_HOME`, `XDG_CONFIG_HOME`
and `XDG_STATE_HOME`. The project was a throwaway git repository seeded through `projects.json`. A
Regular Terminal ran `PS1='$ '; clear; echo hello world`. Only this pass's PIDs were stopped.

## Verdicts

| Check | Before (`226d3a8b`) | After (this branch) | Evidence |
|---|---|---|---|
| A synthetic `xdotool click` on the `w` of `world` leaves no highlight | **FAIL**: the `w` cell stays highlighted | **PASS** | `bug-008-click-before-after.png`, rows 1–2 |
| A click at human pace (press, 0.4 s, release) on `w` after a drag, then Ctrl+Shift+V, pastes the dragged text | **FAIL**: the `w` stays highlighted and `w` is pasted | **PASS**: no highlight; the clipboard still reads `'hello'` and `hello` is pasted | same, rows 3–4 |
| A drag still highlights and copies | — | **PASS**: a drag over `world ` highlights it (row 1) and the clipboard reads `'world'`. An earlier drag over `hello ` set it to `'hello'`, which the paste in rows 3–4 above shows | `bug-008-tap-keeps-clipboard.png`, row 1 |
| After a drag, other text is put on the clipboard (`OTHER-TEXT`, owned by another X client). A synthetic `xdotool click` (press and release in one event batch) on `hello` then leaves that text in place | — | **PASS**: the clipboard still reads `'OTHER-TEXT'`, the highlight is cleared, and Ctrl+Shift+V pastes `OTHER-TEXT` | same, rows 2–3 |

In `bug-008-click-before-after.png`, the red-bordered rows are before and the blue-bordered rows are
after. Every crop is `400x80+300+60` of the full frame, at 200%.

## Not covered

- Double-click and triple-click were not re-run on the display. They are unchanged by this fix: a
  `Word` or `Line` selection is never empty, and U19 (`word_expansion_selects_whole_word`,
  `line_granularity_selects_whole_line`) holds it. `step10f-double-click-word.png` is the earlier
  pass.
- macOS (Cmd+C / Cmd+V) was not run. The change has no `cfg(target_os)` arm.
