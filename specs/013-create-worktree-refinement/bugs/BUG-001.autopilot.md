# Autopilot ledger — 013-create-worktree-refinement BUG-001

Maintained by the `speckit-autopilot` skill. It is the record of what this flow owns and how far it
has got. `resume` finds this file by its **Worktree branch** line and reads it. Keep it true.

- **Input**: Currently when creating new worktree that for example has multiple git submodules may take 30 seconds or more. If user during that time will click somewere out side of the overal then it dispears and there is no information what ever the worktree was created or not. It should not be posible during the worktree creating to lose the focuse and go outside of overlay in other way then explicitly click cancel
- **Kind**: bug
- **Worktree branch**: fix/create-new-worktree-maybe-interupted
- **Started**: 2026-09-19 (adopted; steps 1–3 and the patch/verify were run by hand on 2026-09-17/18)
- **Phase**: 0-bug
- **Next step**: Milestone M1 unit: rebase onto origin/main, continue TDD from U3

## Phase 0 progress

| Step | State | Evidence |
|---|---|---|
| 1 Reproduce on `origin/main` | done | Headless: `add_worktree_dismissal::a_form_whose_create_is_in_flight_is_dismissed_by_neither_escape_nor_the_scrim` fails on unfixed code with `left: Some(WorktreeForm(Cancelled))` |
| 2 Owning spec | done | `specs/013-create-worktree-refinement` (Status: Closed) |
| 3 bugfix-report | done | `bugs/BUG-001.md`, commit `b42d74a5` |
| 4 patch + verify | done | verify: all checks pass (2026-09-18). Bug-rubric review: 2 rounds, no BLOCKER/MAJOR (r1 CLEAN, 6 MINOR; r2 1 MINOR); MINORs fixed except R1-F6 (declined below) |
| 5 Size | done | Stays a bug: FR-010a/b are the spec's FR-006/FR-009 intent (+ 017 FR-009's non-dismissible exception); 10 tasks (T032–T041), not over 10 |
| 6 Milestone | in progress | M1 unit running: U1, U2 committed; rebase and U3 onward next |

## Pull requests

| PR | Purpose | Status | Merge SHA |
|---|---|---|---|

## Milestones

| ID | Tasks | Deliverable | PR | Status |
|---|---|---|---|---|
| M1 | T032–T041 | Create overlay ignores scrim click and Escape while a create runs; a create finishing after Cancel reports its outcome as a notification | — | in-progress |

## Decisions

| # | Phase | Question | Answer | By | Evidence |
|---|---|---|---|---|---|
| D1 | 0-bug | What does Cancel do during an in-flight create? | Closes the overlay; the create continues; its outcome arrives as a notification. No abort. | agent-resolved | spec.md FR-010b; aborting a submodule fetch needs its own rollback design (bugs/BUG-001.md Patch Record) |
| D2 | 0-bug | Adopt the hand-run BUG-001 work into autopilot? | Yes | user | AskUserQuestion 2026-09-19 |
| D3 | 0-bug | Relabel Cancel while a create runs? | No — keep "Cancel" | user | user message 2026-09-19 |

## Declined review findings

| Milestone | Review | Finding | Why declined |
|---|---|---|---|
| — (Phase 0) | Bug rubric r1 | F6 [MINOR]: 10 fix tasks is not "few"; fold T040+T041 into one acceptance task | Optional per the reviewer; 10 is within the Phase 0 size limit (step 5), and T040/T041 are the separate outer-loop tests `/speckit.tdd.plan` derived (Escape/scrim vs. outcome-after-Cancel) |

## Open escalation

None.

## Follow-ups not done

- Reopening the form while a cancelled create still runs lets the reopened form absorb that create's outcome (pre-existing via scrim click; test-list Out of scope).
