# Autopilot ledger — 016-existing-branch-worktree BUG-004

Maintained by the `speckit-autopilot` skill. It is the record of what this flow owns and how far it
has got. `resume` finds this file by its **Worktree branch** line and reads it. Keep it true.

- **Input**: bug: fix the worktree symlink path refusal too
- **Kind**: bug
- **Worktree branch**: fix/010-worktree-include-symlink
- **Started**: 2026-09-14
- **Phase**: done
- **Next step**: none — #342 rebase-merges on a green `ci complete`; GitHub's `state` is the record

## Pull requests

| PR | Purpose | Status | Merge SHA |
|---|---|---|---|
| #342 | BUG-004: BUG record, spec patch, regression test and fix | merged on green | the commit on `main` carrying this ledger |

## Milestones

| ID | Tasks | Deliverable | PR | Status |
|---|---|---|---|---|
| M1 | T093–T096 | The daemon includes and stops showing a worktree named through a symlink, as the worktree git reports | #342 | merged |

## Decisions

| # | Phase | Question | Answer | By | Evidence |
|---|---|---|---|---|---|
| D1 | bug | Which spelling is persisted and answered when a request names a worktree through a link? | Git's own record of the worktree, so `reconcile()`'s exact match against git's records stays true | agent-resolved | `crates/micold-core/src/worktree.rs` `reconcile()`; plan.md BUG-002 amendment |
| D2 | bug | Owning spec in flight? | No — 016 is Closed | agent-resolved | specs/016-existing-branch-worktree/spec.md `**Status**` |

## Declined review findings

None. Patch review (bug rubric, round 1): CLEAN, four MINORs all taken — exclude resolves off the state lock, T093 covers a removed worktree, the contract states what `WorktreeExcluded.path` carries, M1 spans T096.

| Milestone | Review | Finding | Why declined |
|---|---|---|---|
| M1 | B | MINOR: the exclude half of the symlink test was never seen red on its own | Include through the link fails first on `origin/main`, so the test is red there; the exclude half is also covered by the removed-from-disk test. Code review A: CLEAN |
| M1 | B | MINOR: `try_for_each` persists once per matched entry | The stored set holds git's spelling, one entry per worktree, so at most one entry matches |

## Open escalation

None.

## Follow-ups not done

- `crates/micold-daemon/tests/exclusivity.rs` `a_second_open_of_a_held_pi_conversation_starts_nothing`
  fails on this host 10/10 (`launch_count` 0, expected 1), on this branch and on `origin/main` at
  `226d3a8b` alike; `main`'s CI is green there. Not code this flow wrote, so not fixed here.
