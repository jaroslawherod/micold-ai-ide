# Autopilot ledger — 018-material3-visual-system BUG-015

Maintained by the `speckit-autopilot` skill. It is the record of what this flow owns and how far it
has got. `resume` finds this file by its **Worktree branch** line and reads it. Keep it true.

- **Input**: bug: the snackbar "Dismiss" action spills past the snackbar's right edge when the message is long
- **Kind**: bug
- **Worktree branch**: fix/snackbar-dismiss-overflow
- **Started**: 2026-09-14
- **Phase**: 4-milestones
- **Next step**: M1 — open the PR, wait for `ci complete`, merge

## Pull requests

| PR | Purpose | Status | Merge SHA |
|---|---|---|---|
| — | BUG-015 fix | pending | — |

## Milestones

| ID | Tasks | Deliverable | PR | Status |
|---|---|---|---|---|
| M1 | T184–T187 | a long, dismissible snackbar keeps `Dismiss` whole inside its container | — | reviewed, PR pending |

## Decisions

| # | Phase | Question | Answer | By | Evidence |
|---|---|---|---|---|---|
| D1 | bug | Owning spec, and is it free to patch? | 018 — `Snackbar` is its T052 / §7.8; Status Closed 2026-09-13, no open ledger in `bugs/` | agent-resolved | `specs/018-material3-visual-system/spec.md` Status line |
| D2 | bug | Bug path or new spec? | Bug path: the fix restores §7.8's intended anatomy, adds no behaviour | agent-resolved | `contracts/design-tokens.md` §7.8 |
| D3 | bug | Bug-rubric review of the BUG record and patch | CLEAN, five minor findings (misattributed §7.8 quote, 48dp floor unstated in T185, derived figures, no contract entry in traceability, mechanism wording in the §7.8 row); all five fixed | agent-resolved | fresh reviewer subagent, 2026-09-14 |
| D4 | 4-milestones | `speckit-implement` stops on `checklists/requirements.md`'s one unchecked item ("Written for non-technical stakeholders") | Not a gate for this fix: that checklist's notes record it as a regression from 2026-07-27, "accepted as-is", and 018 was planned, built and closed with it; BUG-015 adds no requirement it bears on. Not answered "proceed anyway"; the checklist is left as it is | agent-resolved | `checklists/requirements.md` notes |
| D5 | 4-milestones | 018 has no `tdd/test-list.md`, which `speckit-tdd-run` wants | Follow 018's own bug precedent (BUG-014 Phase 26): the test-first discipline is kept and its evidence is the Red record in `tasks.md`, written before the fix | agent-resolved | `tasks.md` Phase 26 |
| D6 | 4-milestones | Reviews A and B on M1 | Both CLEAN. A: F1 declined (below), F2 fixed with a comment on the clamp test. B: F1 (tick T052 with T187) and F2 (record the daemon test follow-up) both done | agent-resolved | fresh reviewer subagents, 2026-09-14 |

## Declined review findings

| Milestone | Review | Finding | Why declined |
|---|---|---|---|
| M1 | A (F1, minor) | Give the snackbar's `Reflow` a `lead_min` so a narrow window stacks `Dismiss` below the message instead of narrowing the message | Adds behaviour §7.8 never asked for. The requirement is the action whole inside the padded edge, which holds at every width with `lead_min` 0; before the fix the pair never stacked either. Not needed for BUG-015 |

## Open escalation

None.

## Follow-ups not done

- `micold-daemon`'s `exclusivity::one_conversation_one_session::a_second_open_of_a_held_pi_conversation_starts_nothing` fails five runs out of five on this machine (left 0 `pi` spawns), while `origin/main`'s CI at `226d3a8b` passes it. The test is not in this flow's code, and this flow did not investigate it.
- In the snackbar, the zero-width 48dp spacer is one of the row's children, so the row's spacing also lands before the message. The message therefore starts 32dp in from the container's edge, not 16dp. This predates BUG-015 and was left unchanged.
