# Autopilot ledger — 018-material3-visual-system [BUG-016]

Maintained by the `speckit-autopilot` skill. It is the record of what this flow owns and how far it
has got. `resume` finds this file by its **Worktree branch** line and reads it. Keep it true.

- **Input**: "check the screen shot the UI is showning highlight for different kind of items. That was not happening in the past and entered after one of recent changes"
- **Kind**: bug
- **Worktree branch**: fix/highligh-is-shown-when-it-should-not
- **Started**: 2026-09-17
- **Phase**: 4-milestones
- **Next step**: orchestrator waits for `ci complete` on #375, then rebase-merges

## Pull requests

| PR | Purpose | Status | Merge SHA |
|---|---|---|---|
| #375 | BUG-016 fix (M1) | open | — |

## Milestones

| ID | Tasks | Deliverable | PR | Status |
|---|---|---|---|---|
| M1 | Phase 28 | A clicked tab draws no focus ring while the terminal takes the keys | #375 | ci |

## Decisions

| # | Phase | Question | Answer | By | Evidence |
|---|---|---|---|---|---|
| D1 | 0-bug | Which spec owns the defect? | 018 (FR-022a, the shared keyboard wrapper's indicator); 018 is Closed and BUG-015's ledger is done, so not in flight | agent-resolved | `specs/018-material3-visual-system/spec.md` Status, `bugs/BUG-015.autopilot.md` |
| D3 | 0-bug | Bug path or Phase 1? | Bug path: FR-022a clarified (no new behaviour), 4 tasks (T188–T191) | agent-resolved | speckit-bugfix-verify all pass; bug-rubric review found the patch a clarification |
| D2 | 0-bug | Reproduced? | Yes, origin/main 5c371812 on Xvfb + lavapipe: click the `claude` tab, press Space in the terminal, the tab draws the ring | agent | `bugs/BUG-016.md` Reproduction |

## Declined review findings

| Milestone | Review | Finding | Why declined |
|---|---|---|---|
| — | bug rubric | ~~F1 [MAJOR] capture check is order-dependent: a focused button before the pane (sidebar) would still take Space | Not reproducible: sidebar `+` start action and worktree refresh, clicked then Space with the terminal focused, drew no ring and did nothing (Xvfb, 2026-09-17). Recorded in BUG-016 root cause and as a follow-up rather than widening the fix to an app-level unfocus operation~~ Superseded: the user asked for it in #375, done as T192–T193 |

## Review log

- M1 round 1: A (code-review high) and B (conformance) both flagged that the docs commit was built on a stale tree and reverted 031/030/027 work already on main (an `origin/main` that moved under a `git reset --soft`). Fixed by rebuilding the three commits on `15b4493f` so the diff holds only `specs/018-…` and the two `ui/material` files. B's F2 (the gate record named the old base) fixed by re-running the gate (3263 passed). B's F3 (follow-up not tracked) fixed by pointing BUG-016 at this ledger's follow-ups. B's regression check: fails on `15b4493f` with the recorded message, passes on the branch.

- User request (2026-09-17): fix the declined order-dependence follow-up in #375. T192 (Red against a pass-through stub), T193 `cdk::keyboard_elsewhere::KeyboardElsewhere` wired around the window in `ui/mod.rs`, a gallery exemption, gate 3279 passed on `66fae572`, rendered-frame re-run.

- T192–T193 reviews: A (code-review high) no findings. B (bug rubric) CLEAN with two MINOR findings, both fixed: F1 put the second part of the fix into FR-022a, the spec's history note and plan.md; F2 wrote the wrapper's no-field assumption into its module docs.

## Open escalation

None.

## Follow-ups not done

