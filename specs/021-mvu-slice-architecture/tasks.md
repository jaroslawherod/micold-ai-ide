---

description: "Task list for feature 021 — Feature-Module MVU Architecture"
---

# Tasks: Feature-Module MVU Architecture

**Input**: Design documents from `/specs/021-mvu-slice-architecture/`

**Prerequisites**: plan.md, spec.md, research.md, data-model.md, contracts/, quickstart.md — all present

**Tests**: MANDATORY per Constitution Principle I. This feature has an unusual test posture worth
stating once: the **existing 71-file suite is the specification** (spec assumption "Test suite is the
behavior specification"), and FR-027 freezes its assertions. So for *extraction* tasks the Red state
already exists — any behavior drift turns the suite red. New **invariants** (the three guard tests)
follow ordinary Red-Green-Refactor: the guard is written and observed failing first.

**Documentation**: Not user-facing, so Principle VII is satisfied by architectural documentation
(`docs/development/architecture.md`), written per-story rather than deferred to polish.

**Cross-platform**: Principle VI. Only one platform branch is touched (the OS-theme probe), and
porting it improves parity. SC-006 requires all three platforms green.

## Format: `[ID] [P?] [Story] Description`

- **[P]**: Can run in parallel (different files, no dependencies)
- **[Story]**: US1–US4 from spec.md
- Every task names exact file paths

## Path Conventions

Three-crate Rust workspace. `crates/micold-core/` (render-free domain + ports),
`crates/micold-client/` (iced GUI + app state), `crates/micold-daemon/` (**out of scope**, Q1).

## Story-to-tier map, and why the order differs from priority

US1 and US2 are **both P1**. The spec breaks the tie explicitly: US2 "is the outcome the other three
depend on — without per-feature boundaries there is nothing for the overlay registry to register
into". So Tier 1 (US2's first half) goes first, and it is the MVP.

US2 is delivered across **two** phases, because the spec assigns it both feature modules (Tier 1)
and per-feature reducer modules (Tier 3). This is not a numbering accident.

| Phase | Tier | Story | research.md §6 steps |
|---|---|---|---|
| 3 | Tier 1 | US2 (part 1) 🎯 MVP | 1–7 |
| 4 | Tier 2 | US1 | 8–11 |
| 5 | Shell split | US3 | 12–16 |
| 6 | Tier 3 | US2 (part 2) + US4 | 17–20 |

**Every task is its own commit** — SC-009 is verified from git history, not just the endpoint.
A task that needs a later task to compile is a planning error, not an acceptable intermediate.

---

## Phase 1: Setup

**Purpose**: Scaffolding and a baseline to measure against

- [X] T001 Create `crates/micold-client/src/features/mod.rs` with an empty module tree and declare `mod features;` in `crates/micold-client/src/lib.rs`
- [X] T002 [P] Record the pre-change baseline in `specs/021-mvu-slice-architecture/baseline.md`: per-file line counts from `find crates -name '*.rs' -exec wc -l {} + | sort -rn | head -10`, `State` field count, `Message` variant count, and the current commit SHA
- [X] T003 [P] Create `docs/development/architecture.md` with section headings only — tier structure, where a feature lives, adding a floating surface, adding a capability, the read/write asymmetry

---

## Phase 2: Foundational (Blocking Prerequisites)

**Purpose**: The safety nets every later phase relies on

**⚠️ CRITICAL**: No extraction may begin until T004 exists — it is what makes FR-027 enforceable
rather than aspirational

- [X] T004 Add an assertion-freeze check to `scripts/check-assertions-frozen.sh` that fails when `git diff <base>...HEAD -- crates/*/tests/` removes or alters a line matching `assert`, allowing pure relocation (the identical assertion re-added elsewhere in the same diff), per FR-027
- [X] T005 [P] Wire T004 into CI in `.github/workflows/ci.yml` as a non-blocking advisory job first, so its false-positive rate is known before it gates merges — **it earned its keep on PR #98**: two false positives on this feature's own diff (a relocated constant's import path changing, and rustfmt rewrapping an assertion past the width limit). Both fixed by comparing whole balanced assertion statements instead of lines; four probes (delete / weaken / rewrap / no-op) verify it still flags what it should. Had this job been blocking from the start, T023 would have been stuck behind a check that was wrong
- [X] T006 [P] Confirm the baseline suite is green on Linux, macOS and Windows via CI before any change lands — discharged on PR #98, run 31261847685: `build + test` passed on ubuntu, macos and windows. Needed a pull request to exist at all, since `.github/workflows/ci.yml` triggers only on `push: main` or `pull_request`; a feature-branch push runs nothing. The same run caught `fmt + clippy` failing on drift the local loop had not been checking

**Checkpoint**: Behavior drift and assertion tampering are both detectable. Extraction can start.

---

## Phase 3: User Story 2 (part 1) — Feature modules (Priority: P1) 🎯 MVP

**Goal**: Every custom type in the monolithic state file moves into a module named for its feature,
together with the helper functions over it. Tier 1 of research.md §6, steps 1–7.

**Independent Test**: Pick any feature; write a test constructing only that feature's types,
exercising only its own operations, with no reference to any unrelated feature's types. It must
compile and pass without the application shell (SC-004).

**Method for every extraction task below**: move the type *with* its helpers (FR-001), `pub use` it
back from `app.rs` in the same commit so no call site changes, and keep the whole suite green. Never
split a feature across parallel state/update/view files (FR-001a).

**Commit shape — corrected during T016.** The isolation test and its extraction go in **one**
commit. Splitting them, as T007/T015 did, leaves the test commit importing a module that does not
exist yet: `809e7ae` does not compile, which violates SC-009 ("verified by the step's own commit").
The test is still written and observed failing first — that ordering is what TDD asks for — but
Red is recorded in the commit message rather than in the history. Run `cargo fmt --all` before
committing; CI's `fmt + clippy` job checks it and the first extractions did not.

### Tests for User Story 2 — write first, observe failing ⚠️

Each test constructs only its own feature's types, so each fails to compile until its module exists.
All are separate files, so all are parallelizable.

- [X] T007 [P] [US2] Isolation test for the worktree-creation form in `crates/micold-client/tests/features_worktree_form.rs`
- [X] T008 [P] [US2] Isolation test for sidebar types in `crates/micold-client/tests/features_sidebar.rs`
- [X] T009 [P] [US2] Isolation test for project/workspace types in `crates/micold-client/tests/features_project.rs`
- [X] T010 [P] [US2] Isolation test for settings types in `crates/micold-client/tests/features_settings.rs`
- [X] T011 [P] [US2] Isolation test for worktree types in `crates/micold-client/tests/features_worktree.rs`
- [X] T012 [P] [US2] Isolation test for notification types in `crates/micold-client/tests/features_notifications.rs`
- [X] T013 [P] [US2] Isolation test for session types in `crates/micold-client/tests/features_session.rs`
- [X] T014 [P] [US2] Isolation test for daemon-connection types in `crates/micold-client/tests/features_connection.rs`
- [X] T014a [P] [US2] Render-free guard in `crates/micold-client/tests/features_are_render_free.rs` asserting no module under `crates/micold-client/src/features/` names the rendering framework in code — comments excepted, matching the existing convention in `app.rs` (FR-006). Follows the mechanism of `crates/micold-client/tests/{material_boundary,cdk_no_appearance}.rs`. **This is a regression lock, not a migration**: the property holds today and the guard exists to keep it holding across eight extractions. Q2's decision to site feature modules in the client rests entirely on it

### Implementation for User Story 2 (part 1)

Sequential — every task edits `app.rs`, so none of these are parallelizable against each other.

- [X] T015 [US2] Move `WorktreeForm`, `WorktreeFormStatus`, `BranchSource`, `ResolutionState` and their impls from `crates/micold-client/src/app.rs:86–326` to `crates/micold-client/src/features/worktree_form.rs` (~240 lines)
- [X] T016 [US2] Move `SidebarEntry`, `DefaultNode`, `WorktreeNode`, `TagFilter`, `matches_filters`, `worktree_location_label` from `crates/micold-client/src/app.rs:372–456` to `crates/micold-client/src/features/sidebar.rs` (~85 lines) — `DEFAULT_LOCATION_LABEL` travelled with them: it is the project-root half of the same location tooltip, and leaving it behind would split the feature (FR-001)
- [X] T017 [US2] Move `ProjectMenu`, `clamp_menu_anchor`, `SwitcherEntry`, `RenameDraft`, ~~`SelectKind`~~ from `crates/micold-client/src/app.rs:327–371, 457–497` to `crates/micold-client/src/features/project.rs` (~85 lines) — **`SelectKind` did not travel**: it is terminal text selection (feature 006, FR-013) and was swept into this task by its line range, not by its feature. Grouping it under "project" would be the exact mistake FR-001 exists to correct. It goes to `features/session.rs` in T021 instead
- [X] T018 [US2] Move `SettingsDraft` from `crates/micold-client/src/app.rs:469–484` to `crates/micold-client/src/features/settings.rs` (~16 lines) — **the feature stays split after this task**: the draft's validation (range checks and their error messages) lives in `main.rs`'s `Message::SettingsSaved` arm, not beside the type it validates. That is reducer code returning a `Task`, so moving it is Tier 3 work, not Tier 1's. Recorded in the module's own doc comment so the split is visible from the code
- [X] T019 [US2] Move `WorktreeRenameDraft` and the worktree helpers `worktree_tree`, `filtered_worktree_tree`, `visible_worktrees`, `has_visible_worktrees`, `worktree_tags`, `worktree_display_name`, `available_tag_filters` from `crates/micold-client/src/app.rs:498–510, 2156–2245, 2276+` to `crates/micold-client/src/features/worktree.rs` (~105 lines) — **split across two modules, not one.** `WorktreeRenameDraft`, `visible_worktrees`, `has_visible_worktrees`, `worktree_display_name` and `worktree_tags` are worktree-owned and went to `features/worktree.rs`. `worktree_tree`, `filtered_worktree_tree` and `available_tag_filters` went to `features/sidebar.rs`: they are named for worktrees but typed for the sidebar — they return `WorktreeNode`/`TagFilter`, read `sidebar_filters`, and `worktree_tree`'s doc comment opens "Build the sidebar tree". Filing them by name would group against the feature, which is what FR-001 argues against and what SC-010 measures. `sidebar_entries` moved with them (T016 had left it behind). `worktree_tags` widened from private to `pub(crate)` to cross the boundary — revisit in T062
- [X] T020 [US2] Move `NoticeLevel` and ~~`Notification`~~ from `crates/micold-client/src/app.rs:923–944` to `crates/micold-client/src/features/notifications.rs`, reconciling against the existing `micold_core::notify` queue rather than duplicating it (~22 lines) — **the reconciliation was a deletion.** `app::Notification` was never constructed anywhere: every real notification is a `micold_core::notify::Notification` on the queue, which is what the snackbar renders. The duplication the task warned against was already present, not about to be introduced. `NoticeLevel` is not a duplicate and stays — it is the banner's fill vocabulary, while `notify::Level` also decides how long a message lingers (FR-032c). The inline `match` that translated between them became `NoticeLevel::to_queue_level`
- [X] T021 [US2] Move the session helpers `sessions_in_worktree`, `active_sessions`, `switch_active`, `record_foreground`, `restore_after_activation`, `restore_foreground`, `arm_notice`, `note_background_restart`, `session_mut` from `crates/micold-client/src/app.rs:2014–2155` to `crates/micold-client/src/features/session.rs` (~142 lines), **plus `SelectKind`**, reassigned here from T017 — `session_mut` widened from private to `pub(crate)`: seven reducer arms call it and the reducer does not move until Tier 3 (T062)
- [X] T022 [US2] Extract the daemon-connection types and the `connection_status` projection from `crates/micold-client/src/app.rs` and `crates/micold-client/src/main.rs:2106` to `crates/micold-client/src/features/connection.rs` — **this step is absent from research.md §6's Tier 1 table, which lists seven steps for eight features; the gap was found during task generation** (FR-001, as amended). `ConnectionStatus` came from `ui/mod.rs`, not `app.rs` — it sat beside the banner that draws it. `connection_status` now takes the four facts instead of the shell's `App`, so the precedence is testable without a window (Principle I); the active-project-to-displacement lookup stays in `main.rs`, being plumbing rather than a decision
- [X] T023 [US2] Remove the transitional `pub use` re-exports from `crates/micold-client/src/app.rs` and update every call site to import from `crate::features::*` — also removed the one in `crates/micold-client/src/ui/mod.rs` that T022 left. The `app::` references that remain across the suite are `State`, `Message`, `Overlay`, `ClosingOverlay` and `on_escape`, which Tier 1 does not move
- [X] T024 [US2] Write the "where a feature lives" and "tier structure" sections of `docs/development/architecture.md`, listing all nine modules — the ninth is `worktree_form`, not an overlay module; overlays are listed as still living in `app.rs` until Tier 2
- [X] T025 [US2] Verify SC-010 by review — name the single module for each feature in FR-001 — and record the intermediate `app.rs` line count against T002's baseline — **SC-010 does not yet pass, and Tier 1 could never have made it pass**: FR-001 names nine features and `overlays` is one of them, which Tier 2 gives a module. Settings is partial (validation in `main.rs`). The review table is in `baseline.md`; `app.rs` 2,434 → 1,689 (−31%)

**Checkpoint**: `app.rs` should be roughly 1,700 lines (types out, both reducers still in). Every
feature answers "where does it live?" with one module. Full suite green on all three platforms.

---

## Phase 4: User Story 1 — Overlay registry (Priority: P1)

**Goal**: Adding a floating surface costs its own module plus at most one registration line, and
zero edits to any central match statement. Tier 2 of research.md §6, steps 8–11.

**Independent Test**: Add a throwaway overlay end-to-end and count changed files with
`git diff --stat`. It must be its own module plus ≤1 registration line, with zero central match
edits (SC-001). Then revert.

**⚠️ Highest-risk phase in the feature.** The exit-animation snapshot (FR-011) renders a *copy* of a
surface whose live state has been cleared, and `ClosingOverlay` exists solely to serve it. Steps
land as four separate commits so a bisect finds one of them, not a monolithic overlay rewrite.

### Tests for User Story 1 — write first, observe failing ⚠️

- [X] T026 [P] [US1] Registration guard in `crates/micold-client/tests/overlay_registration.rs` — a surface that exists but is not registered MUST fail the build or this test, never be discovered by hand at runtime (FR-010, contract R2) — **guards the seven popovers, not the `Overlay` enum.** R2's premise ("forgetting one of eight edit sites") does not hold for the enum: those sites are matches over a closed enum, so `rustc` already enforces coverage — verified by removing an arm three ways, each of which failed to compile before any test ran. The popovers are loose `State` fields with no such protection, and the two dismissal paths clear different subsets of them (4 of 7 and 6 of 7). The guard makes every combination a recorded decision; three probes confirm it fires
- [X] T027 [P] [US1] Dismissal-ordering test in `crates/micold-client/tests/overlay_dispatch_ordering.rs` covering contract obligations D1 (~~popover closes before modal on Escape~~ — **the contract had D1 backwards**; the popover branch is guarded on no modal being open, so the modal wins whenever both are, and FR-012 says preserve what exists. Contract corrected, test asserts the code), D2 (opening a modal closes popovers) and D3 (closing the filter panel leaves filters intact), asserted against the public entry points rather than the special-case match, so the migration must leave it passing unmodified

### Implementation for User Story 1

- [X] T028 [US1] Introduce the uniform `FloatingSurface` type and ~~`StackBand`~~/`DismissalRules` in `crates/micold-client/src/overlay/mod.rs`, built on feature 017's existing `Layer`/`Surface`/`Trigger` vocabulary in `crates/micold-core/src/overlay.rs` — not a parallel one (FR-014) — **`StackBand` was not introduced**: it is a second name for `micold_core::overlay::Layer`, which already declares the bands bottom-to-top and derives `Ord` as the z-order, and a synonym is exactly the parallel vocabulary FR-014 forbids. `DismissalRules` exists but decides nothing — it records the surface kind and the cancel message and forwards every trigger question to `dismisses`. Also note **`ui::cdk::overlay::Surface` already is a uniform floating-surface type** (feature 017): it owns render-time concerns (panel, anchor, scrim), while this owns state-time ones (identity, which is open, what closes it, and at T036 the snapshot). Three layers, one rule — recorded in the module doc so `one_overlay_implementation.rs` is not read as having been quietly widened. The trait has **no `view` method**, contrary to the contract sketch: FR-006 forbids feature modules naming the renderer, and Tier 1 already sited views in `crate::ui`; T029's registration line names a surface and its view together so FR-009's one-registration-point still holds
- [X] T029 [US1] Add the registry and its `register!` macro in `crates/micold-client/src/overlay/registry.rs`, with `Overlay`/`ClosingOverlay` still present and deriving into it so both representations coexist green — **the derive is `Overlay::as_surface`**, one function on the enum returning each variant's `SurfaceId` and cancel message. Both readers call it: the registry, and `on_escape`, whose nine-arm match is now that one call. So the two representations cannot answer differently while they coexist, and the equivalence is exhaustively tested (`overlay_registry.rs`, twenty states = ten variants x filter panel open/closed). **Two registrations, not sixteen**: `ModalSurface` (the transitional bridge standing for whichever `Overlay` variant is open — T032 splits it into nine) and `features::sidebar::SidebarFilterPanel` (a genuine feature-owned surface, costing one line). Two is enough to exercise both bands and both dispatch shapes; more would have pre-empted T031/T032. **Dispatch has two shapes on purpose**: `escape` goes to the topmost surface only (contract D1), `scroll_beneath` to every surface it reaches (what `dismiss_on_scroll_beneath` does today). A single `dismiss` entry point would have had to pick one and silently change the other. `probes()` is public so contract R3 can be tested by reordering, which is the only way to test it. Two probes confirm the guard fires: demoting the modal to the popover band, and registering a modal with no cancel message
- [X] T030 [US1] Implement the builder API for `FloatingSurface` terminating in `.into()` per Principle VIII and FR-030, and confirm `crates/micold-client/tests/material_builder_api.rs` still passes — it does (10 tests, unmodified), as does `showcase_completeness.rs` (34). **The `.into()` is `Open`, not an `Element`.** Principle VIII's terminator is conversion into an `iced::Element`, and nothing under `src/overlay/` becomes one — T028 removed the `view` method the contract sketch gave `FloatingSurface` precisely so it could not (FR-006). A surface's element terminator is `ui::cdk::overlay::Surface`, already a Principle VIII component, reached at T035. What this task did apply is the *shape*: `Open::of` became `impl<S: FloatingSurface> From<&S> for Open`, so erasure reads `(&surface).into()` and there is exactly one door out; and `tests/overlay_builder_api.rs` now holds the layer to no public fields and no `&mut self` setters, so the sixteen surfaces T031–T036 add cannot introduce a second way to configure one. The scan is `tests/inventory/mod.rs`'s, pointed at another directory via a new `declarations_in` — restating "what a constructor is" in a second scanner is the drift FR-014 objects to. Two probes confirm it fires. **The rule is stated differently here on purpose**: in the library a non-builder public method needs a place on a sanctioned list, but here readers (`kind`, `id`, `layer`, `on`) are the norm, since asking is what generic dispatch does. **Found by the new guard**: `inventory::struct_body` mis-parsed tuple structs — it walked to the next `{`, which for `SurfaceId(&'static str)` is the `impl` block, so every method in it read as a public field. Fixed for all three struct forms with a unit test; the library has no tuple structs, so its gates are unaffected
- [X] T031 [US1] Migrate the 7 ad-hoc popovers off their loose `State` fields onto the registry — `help_menu_open`, `project_switcher_open`, `sidebar_filter_open`, `worktree_menu_open`, `project_menu_open`, `terminal_context_menu`, `session_menu_open` in `crates/micold-client/src/app.rs` (FR-007) — all seven now have a surface type in the feature module that owns them and one `register!` line each. **The fields stay, and that is the migration finishing rather than stalling**: four of them carry payload (which worktree, which session, where the cursor was), which is feature state, not overlay state. What migrated is *openness* — `Registered::open_in` derives it from whatever the feature already stores — so the three pure bools are the only ones the registry could have absorbed, and absorbing them means moving `State` ownership into the feature modules, which is Tier 3 (T062). `open_overlay` and `dismiss_on_scroll_beneath` are now two calls into the registry instead of ten field assignments across two different subsets. **A tenth feature module, `features/help.rs`**: the overflow menu had no home — two constants at the top of `app.rs` and a `bool` two hundred lines below — and FR-001 asks where a feature lives, not how big it is. `HELP_ACTIONS`/`help_actions` moved with it (import-only updates in `ui/toolbar.rs` and `tests/toolbar.rs`; no assertion touched). Architecture doc's module table needs the tenth row at T038. **Three behaviour changes, each argued and pinned by a test:** (1) *Escape now closes every popover*, where it previously closed only a modal or the filter panel. No widget handles Escape — `cdk::overlay::Surface` observes an outside click and nothing else — and feature 017 added `Surface::dismisses_on` precisely so "callers that own such a trigger consult the same rule rather than re-deciding it"; the keyboard subscription was simply never wired to it. FR-012 preserves the *priority* between simultaneously-open surfaces and the modal-closes-popovers rule, both of which still hold and are asserted; it does not require that a surface Escape never reached keeps not being reached. Pinned by `escape_now_reaches_every_popover`. (2) *Opening a modal now closes all seven popovers, not four.* The three it missed were cleared by hand in the reducer arms that open a modal from them — the fragile arrangement T026 recorded — or, for the terminal context menu, by nothing. (3) *A scroll beneath now closes the terminal context menu too*, the seventh of seven; it is a non-modal surface and the core rule has always said so. **A trap worth recording**: several cancellations are toggles whose reducer arms close their neighbours, so a batch of messages collected up front hands a toggle to an already-closed surface and *reopens* it. `close_each` re-asks which surfaces are open after each close, bounded by the registration count. Probed: the batch version fails feature 017's own frozen `overlay_dismissal_delta`. Two further probes confirm R2 fires — an `open_in` reading its neighbour's field, and a missing `register!` line. `overlay_registration.rs` was rewritten to the subject its own T026 doc said it would take: registration, not two lists of field assignments
- [X] T032 [US1] Migrate the 9 real `Overlay` variants onto the registry, preserving each surface's dismissal rules (the 10th variant, `None`, becomes "nothing open" rather than a surface) — nine surface types, each in the feature module that owns the dialog: `AboutDialog` (help — "About" is the Help menu's single action, so the menu and the dialog it opens are one feature), `ProjectSelectorDialog`/`RenameProjectDialog`/`ConfirmForgetProjectDialog` (project), `AddWorktreeDialog` (worktree_form), `SettingsDialog` (settings), `ConfirmWorktreeDeleteDialog`/`RenameWorktreeDialog` (worktree), `ConfirmSessionRemoveDialog` (session). `ModalSurface` — T029's transitional bridge, one registration standing for nine — is deleted. Sixteen registrations now, and `src/overlay/` holds no surface type at all: the layer describes surfaces without containing any, which is the shape SC-001 is measuring. **The enum survives as storage, not as a description**: each dialog's `open_in` is `state.overlay == Overlay::X`, and `on_escape` still matches on it. Those are two independent statements of the same nine facts — the drift this feature exists to end — so `overlay_registry.rs` holds them equal over every state either can express, on *both* the cancel message and the surface identity, until T034 deletes the second. The identity half is new here and is not redundant: T035 keys the view and the exit animation on identity, so a mistyped id would move a dialog's transition rather than break its dismissal, which the message equivalence cannot see. Both probes fire — a typo'd id, and an `open_in` reading its neighbour's variant. **R2 caught a real omission during this task**, not a hypothetical: rewriting the `register!` list dropped `WorktreeContextMenu`, and `every_popover_is_registered` failed on the next build. That is precisely the "opens but will not close, discovered by hand" failure the guard exists for, found by the guard instead
- [X] T033 [US1] Collapse central match site 1 of 6 — the `Overlay` enum at `crates/micold-client/src/app.rs:55` — onto generic dispatch — **site 1's match is `Overlay::as_surface`, and it is deleted**. The enum declaration itself is not a match statement; the match that lives at site 1 is the nine-arm `impl Overlay` block T029 added as the bridge, holding the enum's own account of each dialog's identity and cancellation. That account is now stated once, in the feature module that owns the dialog, so the enum describes nothing: the variants say only *which slot is filled*, and each dialog's `Registered::open_in` reads that. **Site 2's body went with it, necessarily**: `as_surface` had exactly one caller left after T032, `on_escape`, and the two were one match split over two functions — `on_escape` is now `registry::escape(state)`, one line. T034 keeps site 3 (the keyboard subscription's hand-written mirror) and the priority question, which is where the remaining work in that pair actually is; the `on_escape` wrapper survives until then as the name the scrim and ~30 existing assertions call. **The sequencing in the task text is the one thing that could not be honoured**: site 1 is the storage every other site still matches on, so the enum cannot be removed first — that is T037, and the four-commit chain runs 1(bridge) → 3 → 4,5 → 6 → 1(slot). **The equivalence test lost its subject and gained a better one.** With `as_surface` gone there is no second answer to compare against, so `overlay_registry.rs` now states the nine facts itself, in an exhaustive `expected` match written independently of the code under test. Strictly stronger than the equality it replaces: an equality can only catch the two sides *disagreeing*, never both being wrong. Three probes fire — a dialog cancelling with its neighbour's message (caught by the cancellation test only), a typo'd surface id (caught by the identity test only, confirming that half is not redundant), and a tenth variant added with no expectation (fails to compile). The third currently fails at the *library's* remaining match sites before the test file is reached; the test's own exhaustiveness only becomes the load-bearing one at T037
- [X] T034 [US1] Collapse sites 2 and 3 — `on_escape` at `crates/micold-client/src/app.rs:2322` and its keyboard-subscription mirror at `crates/micold-client/src/ui/mod.rs:519` — preserving the popover-before-modal priority currently hand-written at `ui/mod.rs:554` — site 2 went with site 1 at T033; **this is site 3, and it is where T031's Escape change actually reaches the user.** The registry has answered for every popover since T031, but the live keyboard path never asked it: it was a nine-arm match over the enum with the filter panel hand-checked above it. It now emits `Message::EscapePressed` and the reducer asks the registry — *what happened*, not *what should close*, exactly as `ScrolledBeneathOverlay` has worked since feature 017. The two triggers are now reported the same way, and `State::dismiss_topmost` sits beside `dismiss_on_scroll_beneath` as its deliberate opposite shape (one surface vs. every surface it reaches). **The macro is gone, and its reason with it**: `Subscription::filter_map` needs a zero-sized closure and takes the subscription's identity from its `TypeId`, so naming the message per overlay required one distinct closure *expression* each — otherwise iced kept the previous overlay's recipe alive across a switch and Esc emitted the wrong message. A message that does not name its target cannot be stale, so one shared closure is now correct. What stays in the view layer is the only part that belongs to it: whether to hold a listener open at all, which it does only while Escape has something to close, so Esc with nothing open is as inert as before. **The priority is preserved but not "popover-before-modal"** — the task text has it backwards. The hand-written guard read `overlay == None && sidebar_filter_open`, i.e. the popover is consulted *only when no dialog is open*: dialog outranks popover, contract D1. That is now the `Layer` ordering rather than a guard above a match, and `overlay_dispatch_ordering.rs` (which exists to catch exactly this being reversed "to match the contract's prose") passes unmodified. **A `Subscription` is opaque, so the wiring is held from both ends**: `pressing_escape_closes_the_topmost_surface` drives the message the subscription emits and asserts the topmost surface closed *and nothing else moved*; `the_keyboard_subscription_names_no_surface` reads the function and fails if any overlay variant or any per-surface message reappears in it. Both probes fire — reinstating the filter-panel special case, and making Escape close every open surface instead of one. **`on_escape` survives as a name, not a site**: it is one line delegating to the registry, it is what the scrim and ~30 existing assertions across nine test files call, and moving them onto `overlay::registry` would churn frozen assertions to no benefit — `overlay_dispatch_ordering.rs` says in its own header that it "deliberately drives only the public entry points"
- [X] T035 [US1] Collapse sites 4 and 5 — the view match at `crates/micold-client/src/ui/mod.rs:337` and `capture_overlay` at `crates/micold-client/src/main.rs:727` — **site 4 is done; site 5 moves to T036, and could not have been done here.** `capture_overlay` builds a `ClosingOverlay`, and the shape of that snapshot is precisely what T036 decides (`FloatingSurface::snapshot`, contract A1–A3). Collapsing site 5 first would have meant designing T036's type inside T035 and then rewriting it — the two are one decision, so T036 takes both. Same shape of correction as T033/T034: the site numbering in the contract is a checklist, not a dependency order. **Site 4, the ten-arm view match, is gone.** Each dialog's state lookup moved into the `ui` module that draws it, behind a uniform `dialog(&State, ColorScheme, &EnvIncludeOutcome) -> Option<Element>`, and `ui::view` is now `registry::open_dialog(state)` plus a call through the view it was registered with. **The view is named on the registration line, not on the surface** — this is what T028 promised when it removed `FloatingSurface::view`: FR-006 forbids a feature module naming a rendering framework, and Tier 1 sited views in `crate::ui`, so the two halves cannot live in the same module. They can still be named in one *place*, which is what FR-009 actually asks. The `register!` macro grew an optional `=> view` per line (`$(.drawn_by($view))?`), so a dialog is one line and a popover is still one line — popovers register no view, because their panel is pushed by `ui::view` whether or not they are open, since it owns its own fade and must outlive the flag that opened it. The nine `ui` dialog modules became `pub(crate)` so the registration line can name them. **`Open`'s `PartialEq` is now hand-written**, comparing identity/band/dismissal and ignoring the view: deriving it would compare function pointers, whose addresses the compiler does not promise are unique (rustc warns, and clippy's `-D warnings` rejects it) — and which view draws a surface is not part of what makes it that surface. **The identity keying stays on `overlay_key(Overlay)` for now**, contrary to T032's note that T035 would move it: the live dialog and its fading-out snapshot must share one key space or the exit animation restarts, and the snapshot branch is `ClosingOverlay`, i.e. T036. It is a cast, not a match. **Three guards, all probed**: every dialog is registered *with* a view (a dialog registered without one opens and draws nothing, and dismissal, stacking and identity would all still look right); no popover is; and — the failure the first two cannot see — `a_dialog_draws_from_its_own_state` opens each dialog through the reducer and asserts its registered view actually produces a body, so a line pairing a dialog with *another* dialog's view fails here rather than as a modal that renders empty. Seven of nine are covered that way; the project selector's listing and a session's record come from the binary, not the pure core, so a `State` built in a test has neither. Probes fired for a mispaired view and a missing one
- [X] T036 [US1] Collapse sites 5 and 6 — `capture_overlay` at `crates/micold-client/src/main.rs:727` (deferred here by T035) and the `ClosingOverlay` enum and its impl in `crates/micold-client/src/app.rs` — **but the snapshot did not move onto `FloatingSurface::snapshot`, and that is the finding, not a shortcut.** Written out, every surface's `snapshot` would have been the same line: clone the state, remember which surface was open. A trait method whose every implementation is identical is a hook with nothing per-surface in it, and adding one would have made "add a surface" cost a method body again — the opposite of FR-009. So the snapshot is a single type, `registry::Closing`, holding a `SurfaceId` and a boxed `State` clone. **Three per-surface lists collapse to none**: the enum's nine variants, `capture_overlay`'s ten arms, and `ui::dismissing_dialog`'s nine — one idea ("the state as it was") had been spelled out three times, once per site, which is why sites 5 and 6 were always one decision. **The exit now draws through the same registration as the entrance**: `Closing::surface()` runs `open_dialog` over the snapshotted state, so the fading dialog is rendered by the very function pointer the live path calls, rather than by a second renderer that resembles it. `restart_on` moved off `overlay_key(Overlay)` onto the surface's own identity (`surface_key`, FNV-1a over the `SurfaceId` name) — the keying T035 deliberately left behind because the live dialog and its snapshot must share one key space, and they now do by construction. **Three deliberate behaviour changes, all in the exit animation, all improvements the old shape could not express**: (1) a confirm-worktree-delete fading out now shows its branch line, its keep-branch checkbox and any rename override — `ClosingOverlay::ConfirmDelete(String)` carried only the directory and its arm reconstructed a stripped-down dialog, with a comment saying so; (2) confirm-session-remove reads the session record, still present in the snapshot, instead of a separately captured label; (3) a focused field keeps its focus ring through the fade, where `dismissing_dialog` passed `None` for focus everywhere and the ring vanished a frame before the dialog did. **Cost, stated plainly**: a whole-`State` clone before every message *while a dialog is open*, where it used to be a draft clone — the same gate as before, so nothing is cloned in the common case, and the heaviest thing in `State` is the `WorktreeForm` the old enum already boxed for being several times the size of its siblings. **`overlay_transition_identity.rs` had to be restated, and T037 says it must pass unmodified** — see the note against T037. Four probes, each firing on its own property and nothing else: every snapshot reporting one name (3 fail — faithful, injective, and the new draws-itself check); a dialog whose snapshot is never taken (5 fail); a snapshot that keeps no state (2 fail — contract A1 and A3, and *only* those); and a dialog registered without its view (1 fail here, 2 in `overlay_registry.rs`). Full workspace suite green (182 binaries), clippy `-D warnings` clean
- [X] T037 [US1] Delete the `Overlay` enum and confirm `crates/micold-client/tests/{one_overlay_implementation,overlay_dismissal_delta,overlay_stacking,overlay_transition_identity}.rs` all pass **unmodified** — **the enum is gone; two of the four protected tests could not pass unmodified, and the reason is the same one T036 hit.** `one_overlay_implementation.rs` and `overlay_stacking.rs` are untouched and pass — the latter was never at risk, since its `Overlay` is `ui::cdk::overlay::Overlay`, the render-time container, not the state enum. The other two named the state enum as their way of *saying which dialog*, and a test cannot keep text that names a deleted type. Both were restated with every assertion preserved and none weakened (each file's header is the explanation the freeze check is flagged against): `overlay_dismissal_delta.rs` records which gestures close which surfaces and is not about the enum at all, so its nine variants became nine rows of "how to open this dialog" and its `state.overlay` reads became `registry::open_dialog(&state)`; `overlay_transition_identity.rs`, already restated at T036, needed the same substitution in its row list. **What the enum actually was, and what replaced it**: a single slot naming which of nine dialogs was showing — and eight of the nine already had a field of their own saying exactly that (`selector`, `rename_draft`, `worktree_form`, `settings_draft`, and the four confirm-dialog targets), kept in step with the slot by hand at twenty-five reducer sites. Each dialog's `open_in` now reads its own state; About, the one dialog with none, gained `State::about_open`. Net zero fields on `State`. **The one thing the enum bought that a field cannot** (FR-015, Principle V): two dialogs open at once was unrepresentable. `open_overlay(Overlay::X)` *was* the assignment that opened a dialog, and one slot cannot hold two — so it closed the previous dialog for free, while quietly leaving that dialog's draft behind, since nothing read a draft the slot contradicted. With the slot gone a leftover draft *is* a second open dialog, and `open_dialog` picks by band with both in the same band, so which one gets drawn would be whichever the registry saw last. The invariant is therefore a mechanism: `open_overlay` became `State::clear_for_dialog`, which closes any open dialog through the same registry call that closes the popovers (`close_dialogs`, a sibling of `close_popovers` — still no list of dialogs anyone maintains), and the eight call sites that ran it *after* preparing their dialog were reordered to run it before. Two new guards hold it: `one_dialog_at_a_time` over all 81 pairs, and `the_reducer_opens_a_dialog_through_that_mechanism`, which drives real messages so an opening arm that forgets the call fails. **A guard fired during the work and was widened rather than exempted**: `overlay_registration.rs` scans `State` for popover-shaped fields and demanded `about_open` be registered as a popover. Its header had said the compiler holds R2 for the dialogs — which T037 is precisely what stops being true — so the file now records that every dialog is a loose field too, states honestly why the exposure differs (an unregistered popover opens and cannot be closed, because `ui::view` pushes its panel from the flag; an unregistered dialog since T035 is simply not drawn), and adds `DIALOG_FLAGS` plus `a_dialog_flag_registers_in_the_dialog_band` so a popover-shaped field must be classified as one or the other and a claimed dialog must really open one. **Behaviour-neutral**, with one unreachable-state repair: opening dialog B while A's draft was still set used to leave A's draft behind, and now closes A properly. Not reachable through the UI — a modal's scrim swallows every click that is not its own — which is why the enum could hide it. Three probes, each firing on its own half: `clear_for_dialog` no longer closing dialogs (both new guards, and only those two); the `SettingsOpened` arm forgetting the call (only the reducer guard, confirming the two are not redundant); a dialog reading its neighbour's state (8 of 17). Full workspace suite green (183 binaries), clippy `-D warnings` clean
- [X] T038 [US1] Write the "adding a floating surface" section of `docs/development/architecture.md` — four numbered steps (describe the surface in its feature module; write the dialog's view in `crate::ui`; one `register!` line; open it through `clear_for_dialog`), a "what you do *not* do" note, and a table of the six guards with what each would catch. **Also the four corrections Tier 2 landing makes to the rest of the file**, three of which the task text does not mention but which the doc would now be lying without: the tier table said Tier 2 was pending; "Answering where does this feature live?" said *overlays cannot* be answered by naming a module; and "What is still in `app.rs`" listed `Overlay` and `ClosingOverlay`. The fourth is the one T031 asked for — the module table's tenth row, `features/help.rs`, plus a row for `overlay/` itself, since the surface type and the registration list are what finally give overlays a home. **Three claims were corrected against the code rather than written from memory**, which is the point of checking a doc the same way as a test: the dialog-view example said `pub(crate) fn` when the functions are `pub` inside a `pub(crate) mod`, and it now also records that the signature is uniform because the registration line stores it as a function pointer; the opening sentence listed the snackbar as a registrable surface when it is pushed directly from `state.notify` and has nothing for a registration to say; and "a popover's panel is pushed whether or not it is open" is true of the three panel popovers but not the four context menus, which are anchored at a cursor position that only exists while open — the guard rationale survives either way, since both are drawn from the feature's own field and only the registry closes them. **The one gap is stated rather than papered over**: an unregistered *dialog* has no guard, and needs none, because since T035 a dialog is drawn only through its registration — it fails the first time anyone opens it instead of trapping the user behind a surface with no exit. Every named test and code snippet in the section was checked to exist verbatim; no code changed, so the suite is untouched
- [X] T039 [US1] Add the **permanent** SC-001 guard in `crates/micold-client/tests/surface_registration_cost.rs`, failing if any registered surface is reachable from anywhere beyond its own module and the single registration point (SC-001, SC-002a — clarified 2026-08-07: a permanent guard replaces the one-time file count, which proves the property only on the day it is taken) — **four tests, because the task text's one sentence has two holes in it.** Read literally it is a path scan, and a path scan misses the two ways a surface can be depended on without its path appearing: a glob import (`use crate::features::help::*` and then a bare `HelpMenu`) and a central match written over `open.id().as_str()` instead of over types, which names no surface *type* at all. Each gets its own test — `nothing_glob_imports_a_feature_module` and `no_file_enumerates_surfaces_by_id` — rather than making the path scan cleverer, since "no file reaches wholesale into a feature" is worth holding on its own. **Why reachability is the whole of SC-001**, and not just half of it: a surface only its own module and the registry can name is a surface nothing else can be special-cased on, so if `ui/mod.rs` cannot write `AboutDialog` it cannot grow an arm for the About dialog. The "zero central match statements" clause follows from the reachability clause rather than needing a count of its own. **The list is parsed out of `register!`**, not restated here — that is what makes the guard permanent rather than a second list to keep in step, and a surface added next year is covered by a file nobody remembers. **`the_registration_list_is_actually_being_read` is the load-bearing test**: every other one iterates the parse, so a parse returning nothing passes them all vacuously. It holds the parse against `registry::probes()`, the runtime article. Probe 5 below is the demonstration — with the macro reformatted onto one line the path scan passes on an empty list, and only this test notices. **A comment I wrote was wrong and the code was fixed to match it, not the other way round**: it claimed `use features::help::{A, B}` was covered by the straight path match, and it is not — that form writes `help::{AboutDialog`, and it is exactly what rustfmt produces the moment a second item is imported from one module. `names()` now handles both forms; probe 2 fires only because of it. **Matching paths rather than bare identifiers was forced by the measurement, not chosen for elegance**: fifteen of sixteen surfaces were already confined to their module and the registry, and the sixteenth, `ProjectSwitcher`, appeared in `showcase/` — as `showcase::state::Floating::ProjectSwitcher`, a same-named enum variant in the second binary. A word-boundary scan would have failed on day one against a type that has nothing to do with the registry. **The id test counts two, not one, for the same reason**: a surface id is an ordinary lowercase word and `showcase/sections/atoms.rs` has a button labelled "settings". One literal is a coincidence; two is a list — and a feature module is allowed the ids of the surfaces it owns, since holding five of its own is not enumerating anything. **`inventory::sources_under` added** rather than writing a second recursive scanner beside the non-recursive `sources_in` (FR-014); comments are stripped through the shared `code_only`, so prose may name a surface and documentation is not a violation. Five probes, each firing on exactly its own test: a single-item import, a braced import, a glob import, an id-keyed central match, and the blinded parse. Full workspace suite green, clippy `-D warnings` clean
- [X] T040 [US1] Perform quickstart.md procedure M2 (six manual overlay behaviors) and record the result — **three pass, one partial, two not established, and the two that are not are the interesting half.** Recorded in full under quickstart.md §M2. Run on Xvfb + lavapipe via the repo's `visual-pass` skill rather than by a human, with `XDG_DATA_HOME`/`XDG_RUNTIME_DIR` pointed at a scratch directory and a throwaway two-project, 21-worktree fixture, so the developer's own app and daemon were never involved. **Pass**: filters survive closing the filter panel (FR-013); Escape over a switcher+context-menu pair takes the context menu and leaves the switcher (FR-012, D1); opening a modal closes the popover beneath it (FR-012, D2). **Not claimed**: steps 1 and 2, the exit animation. The dialog closes and reopens correctly, but a loop sampling the dialog region every ~60 ms across 40 frames caught *no* intermediate frame of a designed 200 ms exit — the region's mean jumps straight from 9971.78 to 5225.67. Either a software rasteriser with no presentation timing is not playing the transition, or it finishes far faster than its token; a screenshot pipeline cannot tell those apart, so neither step is marked passed. What step 2 *does* establish is the failure that would outlive a bad transition — reopening immediately after Escape gives a single dialog byte-identical to a clean open, so no duplicate, ghost or stuck scrim. **Step 6 is blocked by a finding, not by the environment**: with a popover open the sidebar does not scroll at all — three wheel notches changed **0 pixels**, against the same gesture scrolling normally with no popover — so the trigger never fires and the dismissal cannot be observed. Not a Tier 2 regression: `Message::SidebarScrolled` calls `dismiss_on_scroll_beneath()` correctly, and the cause points at feature 017's overlay primitive capturing wheel events before the content beneath sees them. **And the observation that came with it**: `Message::ScrolledBeneathOverlay` is declared and handled in the reducer but **emitted by nothing** in `src/` — the live producer is `SidebarScrolled(offset)`, whose arm calls the same method — so feature 017's "non-modal surfaces gained scroll dismissal" is asserted only through an entry point the running application never uses, by `tests/overlay_dismissal_delta.rs`. Equivalent *if* the live message is ever sent, which is precisely what step 6 could not confirm. Both belong to feature 017 and are recorded for follow-up rather than fixed here

**Checkpoint**: 19 enum variants and 7 loose fields gone. Six central match statements reduced to
zero. Every pre-existing overlay test passes unmodified.

---

## Phase 5: User Story 3 — Capabilities and shell split (Priority: P2)

**Goal**: Every I/O concern is a narrow declared capability; the binary is the single place real
implementations are chosen; the shell divides by external system. research.md §6, steps 12–16.

**Independent Test**: For each capability, run the behavior depending on it against a fake and
assert the outcome, with no real filesystem, repository, clipboard or OS query involved (SC-005).

**Scoping note**: FR-017 already largely holds — `app.rs` constructs no concrete implementation, and
all nine construction sites are already inside the shell. The real work is FR-018 (single assembly
point); T048's guard is a regression lock on an existing property, not a migration.

### Tests for User Story 3 — write first, observe failing ⚠️

- [X] T041 [P] [US3] Guard in `crates/micold-client/tests/no_concrete_implementations.rs` asserting non-shell code names no concrete implementation — `GitCli`, `JsonFileStore`, `JsonFileSettingsStore`, `StdFolderScanner` — and that they are constructed in exactly one place (FR-017, FR-018) — **the two clauses are in different states, and the task text and its own scoping note disagree about that.** The note calls T041 "a regression lock on an existing property, not a migration"; the text also demands "constructed in exactly one place", which is false today. Both are written at full strength rather than one being chosen. FR-017 **holds and passes**: every mention of a real implementation is already in `main.rs`. FR-018 **does not** — five real implementations are named from eleven places, and `each_implementation_is_chosen_in_exactly_one_place` is `#[ignore]`d with the reason and T049 named, so the requirement stays written at the strength it should pass at and `cargo test` reports it outstanding every run. Observed failing first, which is what the phase heading asks: ``GitCli` is chosen in 2 places`, ``JsonFileSettingsStore` in 3`, ``StdFolderScanner` in 4`. **This introduces `#[ignore]`, which the repository had not used anywhere** — flagged rather than slipped in; the alternative was pinning the assertion to today's count, which would go green and stop being about anything. **The implementation list is derived, not hardcoded, and deriving found a fifth**: scanning `micold-core` for `impl <Port> for <Type>` over the seven ports yields `ClaudeProvider` alongside the four T041 names. The task's list was already incomplete when written. `JsonFileStore` and `ClaudeProvider` are each named once and already comply, which is why the failure names three types rather than five. **`is_shell` already knows about `shell/`**, which does not exist until T050 — a guard that has to be relaxed to let a refactor through is not holding anything. Comments are exempt via the shared `code_only`, since `main.rs`'s own module doc explains the boundary by naming `GitCli`. **Four probes, and two of them were wrong the first time**: the initial attempts injected code that could not compile (a trait method with no trait in scope; a port impl missing two methods), so no test ran and both looked like passes. A third injection was a silent no-op — the anchor line did not exist and `str.replace` does not complain. Redone with assertions, all four fire on exactly their own test: a feature module naming `StdFolderScanner` (FR-017), a reformatted `impl` line (the vacuity guard, proving the other three would otherwise pass on an empty list), a new `Fake`-prefixed port implementation, and the FR-018 countdown. Full workspace suite green, clippy `-D warnings` clean
- [X] T042 [P] [US3] Fake-coverage test in `crates/micold-client/tests/service_capability_fakes.rs` asserting every declared capability has a fake and at least one test exercising real behavior through it (FR-019, SC-005), **and** that each capability is narrow enough that no consumer must implement an operation it does not exercise — the spec's own narrowness test, applied per capability (FR-016). A capability failing the narrowness check MUST be split rather than the check relaxed — **and one does, in two ways, which is the finding.** Three clauses. *Coverage* (FR-019): three of seven capabilities have a fake; `ProjectStore`, `SettingsStore`, `FolderScanner` and `AiCliProvider` do not. Red until T048, `#[ignore]`d at full strength rather than reduced to the three that pass, same arrangement as T041's FR-018 clause. *Exercised* (SC-005): every fake that exists is named by a test — passes today and keeps its grip as T048 adds more, because a fake nobody constructs is an unused type that happens to compile, not evidence a capability is fakeable. *Narrowness* (FR-016): Red, with **six violations across two capabilities** — `FolderScanner::list_subdirs` supplied unexercised in three test fakes, `ProjectStore::save` in three more. Both must be split per this task's own text; that belongs to T046/T048, so it is recorded, not fixed here. **The detector is the contract's wording made mechanical**: `contracts/service-capabilities.md` says "if a test must implement a method it does not exercise merely to satisfy the trait, the capability is too wide", and a method whose parameters are *all* `_`-prefixed and whose body never mentions `self` can only return a constant — it is a signature being satisfied, not a behaviour. The predicate discriminates rather than always firing, which one file demonstrates on its own: `FakeScanner` reads `self.git`/`self.available` in two methods (passes) and returns `Ok(vec![])` in the third (fails). **Scoped to implementations written in tests**, which is what the contract's wording is about — a shared core fake implementing everything once is the relief FR-019 exists to provide, not an FR-016 breach. **The six bespoke fakes exist *because* of the coverage gap**: the four missing shared fakes are why three `FakeScanner`s and three `FakeStore`s were hand-rolled, so T048 should delete them rather than leave them beside the new ones. **`PORTS` and the `impl <Port> for <Type>` derivation moved into `tests/inventory/`** and T041 was rewired onto them: two guards now ask what a capability is, and two answers is the drift FR-014 objects to. **A blind spot found by probing and left recorded rather than papered over**: the scan is line-based, so an `impl` header rustfmt has wrapped across lines is invisible to it — wrapping one took the count from six to five and tripped `the_scan_finds_the_implementations_that_exist`, which is exactly the vacuity guard's job. A first vacuity probe (double spaces in the header) *failed to fire* because the scan trims and is tolerant of it; recorded because a probe that does not fire proves nothing about the guard and had to be replaced with one that does. Full workspace suite green, clippy `-D warnings` clean
- [X] T043 [P] [US3] Behavior test for env-include resolution through its fake in `crates/micold-core/tests/env_include.rs` — **T046 had to land with it, because a test of a fake that does not exist is not a red test, it is a compile error.** Unlike the Phase 3 red tests, which failed on an unresolved import of a module being *moved*, `EnvIncludeResolver` and its fake had never been written; a test naming them breaks the build, which is not mergeable and not what "observe failing" means. So the capability was declared first and the behaviour test written against it, in TDD order but one commit. **The port is deliberately narrower than the contract's sketch.** `contracts/service-capabilities.md` proposes `fn resolve(&self, cwd: &Path) -> EnvIncludeSnapshot`, which folds the *decision* — is the feature on, is the path blank — into the port. That costs twice: the real implementation must then carry settings, so it cannot be a unit struct; and a fake cannot answer the question a test most wants to ask, which is *what was I asked to source, and was I asked at all*. FR-016 wants a capability to be the I/O need and nothing more, so the port is `resolve(path, cwd, timeout) -> (vars, outcome)` and the decision is `snapshot_for`, a pure function beside it — testable with no port involved. **The fake records its calls, and that is the point.** The short-circuit's claim is not "the outcome is `Disabled`"; it is "no subprocess was spawned". Those come apart — an implementation that sourced the script and threw the result away satisfies the first and violates the second — and the probe confirms it: making the short-circuit source anyway fails both *spawns-nothing* tests while every outcome assertion still passes. **This file exists beside two that already test env-include**, and says why: `env_include_resolve.rs` spawns real subprocesses on purpose (FR-005 requires actually sourcing), `env_include_diff.rs` covers the pure diffing, and neither can reach the decision without paying for a shell to observe something that has nothing to do with one. **`EnvIncludeResolver` was added to `inventory::PORTS` the moment it existed** — a capability the guards do not know about is one FR-017 and FR-019 are not holding — and T041's `Fake` exclusion list rejected `FakeEnvIncludeResolver` until it was named there, which is that guard doing its job. Three probes, each firing on exactly its own tests: sourcing despite the short-circuit (both spawns-nothing tests), judging blankness without trimming (the whitespace test), and trimming the path before sourcing (the not-trimmed test). Full workspace suite green, clippy `-D warnings` clean
- [X] T044 [P] [US3] Behavior test for the OS theme probe through its fake in `crates/micold-core/tests/os_theme.rs` — **landed with T047 for the same reason T043 landed with T046**: the trait and fake had never existed, so a test naming them is a compile error rather than a red test. **What it adds beside `tests/theme.rs`, which already covers the fallback**: that file hand-writes `Result` values into `observe_system_scheme`, which covers the decision perfectly and can say nothing about two things — whether the probe is substitutable at all (a test that hand-writes the `Result` never touches the capability), and *sequences*. BUG-001 is not a claim about one reading; it is a claim about a timeline — dark, then a timeout, then the screen must not move — and hand-feeding one value cannot express "and then". So this drives the composed path, probe → observe → repeat, over scripted timelines with zero OS access. **The fake counts how often it is asked**, because a consumer that caches when it should poll still produces a plausible scheme; the count is the difference between the right answer and the right answer for the right reason. Three probes, each firing on its own tests: the fallback decaying to `Unspecified` instead of holding (the BUG-001 regression), the fallback becoming *always* hold so a real change is ignored, and the port collapsing a failure into `Unspecified`
- [X] T045 [P] [US3] Test asserting a feature emits `Outcome::ClipboardWrite` with zero real clipboard access in `crates/micold-client/tests/clipboard_request.rs` (FR-015a, contract C2) — **landed with T056, and the pairing is forced twice over.** Once for the reason T043/T046 and T044/T047 record: `Outcome` had never been written, so a test naming it is a compile error, not a red test. And once more because the enum's own task is T065, in *Phase 6* — so T045 and T056 sit in Phase 5 asking for a type that, by the task list's own ordering, does not exist until after Phase 5's checkpoint. One of the three is wrong about when the vocabulary begins, and the resolution is the narrowest one available: introduce it here with **only** the variant FR-015a authorises. **This is not the Tier 3 work SC-004b forbids, and the distinction is in the spec, not in convenience.** FR-021 sits under "cross-feature coordination" because its subject there is a feature reducer returning a consequence for the *root* to interpret (FR-022) and route to another feature; FR-015a borrows the vocabulary for something else entirely and names the **shell** as interpreter. What landed has no root interpreter, no reducer split, and no feature learning anything about another — Phase 5 stays demonstrable with zero Tier 3 merged. T065 gains the other three variants and the draining interpreter; **its text needs one edit, from "introduce" to "extend"**, noted there. **One emitter, not two, and the second site says why.** `selection::copy_request` is the real one: it takes over the two decisions the shell made inline one line above the write — is anything selected, and does it resolve to text worth writing — which is precisely the rule FR-017 objects to keeping where nothing can reach it without a window. `TextCopyRequested` is left as a shell relay: its text was already named by the view (`worktree_menu_items` asks the worktree feature for the display name), so a `worktree::copy_name_request` wrapping a string in a variant would be ceremony added solely so a test could say "a feature emits". **The paste is deliberately untouched** — the contract says reads arrive back as an ordinary message "exactly as today", so T056's "three direct calls" is two: `clipboard::read` is a shell call no feature requests. **An edge case preserved rather than quietly changed**: `selected_text` returned an empty string when the displayed session had no cached grid, but a multi-line selection resolved against an absent provider comes back as `"\n"`, which is not empty — so the grid lookup stays in the shell (`selection_copy_request`) and hands over nothing at all rather than a selection it cannot resolve. **Six probes, six distinct failure sets**: dropping the emptiness check (2 tests), flattening the newline (1), making "no selection" clear the clipboard (1), a feature reaching for `clipboard::write` (the C1 guard), the shell no longer writing (the C1 guard's vacuity check), and an `if` inside `interpret` (the C3 guard). **Probing cost two rounds of rework and the lesson is now doubly recorded**: the probe script restores with `git checkout` — correct, per T044 — which silently reverts *uncommitted* work, so the change has to be committed before probing, and a `git stash`/`pop` cycle in between unstaged it and let a commit go out with only the test file in it. The anchor-count assertion caught both. Full workspace suite green at 193 binaries, clippy `-D warnings` clean, assertion freeze "OK — no assertion removed"

### Implementation for User Story 3

- [X] T046 [US3] Declare `EnvIncludeResolver` in `crates/micold-core/src/env_include.rs` with a real implementation moved from `crates/micold-client/src/main.rs:397–450` and a fake — **done with T043, which could not compile without it** (recorded there in full). `EnvIncludeSnapshot` moved to the core with the decision that produces it; `SubprocessResolver` is the real implementation and is named only in `main.rs`, so T041's FR-017 guard covers it; `FakeEnvIncludeResolver` records every `(path, cwd, timeout)` it is asked for. The client's `resolve_env_include` is now a thin call into `env_include::snapshot_for` picking the real resolver — the one choice that is the shell's to make. Behaviour-neutral: the short-circuit and the delegation are the same logic in a new home, and the engine's real-subprocess tests are untouched
- [X] T047 [US3] Declare `OsThemeProbe` in `crates/micold-core/src/os_theme.rs` wrapping the `dark_light` call at `crates/micold-client/src/main.rs:2678` with a fake — this is the codebase's only direct OS branch, so this also serves Principle VI — **the trait and fake are in the core; the wrapping is not, and moving it there would have defeated the task's own purpose.** `micold-core` has no dependency on the OS-detection crate, deliberately: that is stated at the top of `theme.rs` and is the entire reason `SystemScheme` mirrors `dark_light::Mode` instead of re-exporting it. Wrapping the call *in the core* would add `dark-light` to the render-free half in order to "isolate the OS branch", which is the opposite of isolating it. So the split follows the port pattern exactly: trait and `FakeOsThemeProbe` in the core, needing nothing but `SystemScheme`; `SystemThemeProbe`, the real implementation, stays in `main.rs` where `dark-light` already is and where FR-017 says a concrete implementation belongs. FR-019 is satisfied — the fake is in the core beside the capability, because the capability is there. **Principle VI comes out stronger**: the only direct OS call now sits behind an abstraction every consumer can be tested against without an OS. **The fallback deliberately stays out of the port** (FR-016): deciding what to do about a failed probe is not an I/O concern, it is `theme::observe_system_scheme`, pure and already tested. **One consequence recorded at both guard sites**: because the real implementation lives in the client, `no_concrete_implementations` — which derives real implementations from the core — never sees it, while the coverage guard finds its fake normally. The type is defined in the shell, so it cannot leak; noted rather than left as a surprise
- [X] T048 [US3] Add fakes for any of the seven existing ports lacking one — `ProjectStore`, `SettingsStore`, `FolderScanner`, `TerminalBackend`, `TerminalHandle`, `AiCliProvider` — in `crates/micold-core/src/` beside each capability, as ordinary public items matching `FakeGit` at `crates/micold-core/src/git.rs:467`. **Not** behind a `cfg` feature and **not** in a separate crate (FR-019, clarified 2026-08-07) — **four were missing, not six**: `TerminalBackend` and `TerminalHandle` already had `FakeTerminalBackend` and `FakeHandle`, so the task's list was two names longer than the gap. `FakeProjectStore`, `FakeSettingsStore`, `FakeFolderScanner` and `FakeAiCliProvider` landed beside their capabilities, and T042's coverage `#[ignore]` came off. **`Mutex`, not `RefCell`, for the two store fakes** — the daemon boxes its stores as `Send + Sync`, and a fake that cannot cross that bound is a fake half the suite cannot use. **T042's two narrowness findings resolve differently, and that is the substance of this task.** `FolderScanner` **was** genuinely too wide, in production and not merely in tests: every consumer that took it as a trait — `Workspace::open_or_activate`, `Workspace::refresh_availability`, `Catalog::add_project` — asks only `is_git_repo`/`is_available`, while the sole caller of `list_subdirs` reaches for `StdFolderScanner` concretely and never goes through the port at all. Split into `FolderScanner` + `FolderBrowser`. `ProjectStore` **was not**: `Catalog`, its only trait consumer, exercises all three operations, and the three hand-rolled `FakeStore`s that had to stub `save` existed because no shared fake did — a coverage gap presenting as a width symptom. Splitting a capability its consumer genuinely uses would have made the codebase worse to score a check green, which is the inverse of what FR-016 is for. **This reverses what I told the user before reading the consumers**, and the evidence is the call sites. **All six bespoke fakes deleted** (three `FakeScanner`, three `FakeStore`), and the three daemon tests moved off `JsonFileSettingsStore` onto `FakeSettingsStore` too — which also took three `tempfile::tempdir()` calls and their real disk writes out of the suite. **A finding recorded rather than fixed**: `AiCliProvider` has a fake but **no substitutable consumer** — `terminal::launch_args`, the daemon's `catalog`/`supervisor`/`state` and the client's `main.rs` all name `ClaudeProvider` concretely, so there is no call site a second provider could be handed to. SC-005 is therefore weaker for this one capability than the other nine, stated plainly in `crates/micold-core/tests/ai_cli_provider_seam.rs` rather than papered over; T049's assembly point is where it becomes answerable. **T048 emptied the thing T042's vacuity guard counted.** That guard required at least six port implementations under `tests/`; there are now zero, by design. A count of violations-era implementations cannot guard a check whose correct answer is an empty list, so it was replaced by `the_detector_can_tell_a_stub_from_a_behaviour`, which runs `methods_of` over a source holding one stub and one behaviour — stronger, because it fails when the parser breaks rather than only when the corpus shrinks. **Probing found two real holes, both fixed rather than noted.** (1) `port_impls_under` strips a qualified path while `methods_of` reconstructed the bare header, so `impl micold_core::fs_scan::FolderScanner for X` was reported as an implementation whose methods were then never examined — a wide fake written that way passed silently. `impl_block_start` now reads the header the same way, and the qualified form is asserted. (2) The SC-005 "exercised" check used `contains`, so adding `"FakeAiCliProvider"` to the known-fakes **allow-list** in `no_concrete_implementations.rs` satisfied it with no test constructing the fake anywhere; it now requires `Fake…::` or `Fake… {`. **The detector's own fixture tripped the detector** on first run — a literal `impl FolderScanner for Probe` inside a test string is read by the file scan — so the fixture names a non-port; recorded because it is the scan proving it is not vacuous. **Six probes, six distinct failure sets**: a fake renamed out of the `Fake` convention (coverage), a hand-rolled wide fake with a qualified header (narrowness), the stub predicate blinded (the detector guard), the seam test deleted (SC-005), `is_available` always true (three workspace tests), and `read_title` ignoring the project (the provider's composition test). **Four assertion-freeze entries, none a relaxation, all in the file T042 itself wrote**: one genuine deletion (the `>= 6` count, replaced by the stronger guard above) and three failure-*message* edits — two because they named T048 as future work that is now done, one because the predicate it describes was strengthened from "names" to "constructs". Full workspace suite green at 194 binaries, clippy `-D warnings` clean, `cargo fmt --check` clean
- [X] T049 [US3] Create the `Capabilities` struct in `crates/micold-client/src/shell/capabilities.rs`, assembled once at boot, replacing all nine inline construction sites in `crates/micold-client/src/main.rs` (523, 532, 649, 1295, 1310, 1330, 1924, 2604, 2709) — four of which are inside `update_inner` (FR-018) — **eleven, not nine**: two more capabilities arrived after this task was written (`SubprocessResolver` at T046, `SystemThemeProbe` at T047), and the line numbers had all moved. `Capabilities::real()` is the assembly point; `App` holds one, because four of the sites are inside `update_inner`, which takes `&mut App` and nothing else. **T041's FR-018 clause now passes unchanged** — written at full strength and `#[ignore]`d since T041, the attribute came off without touching the assertion — and with T042's two cleared at T048, **no `#[ignore]` remains anywhere in the suite**. **`Arc`, and `Capabilities` is `Clone`**: the folder listing runs inside `Task::perform`'s `async move`, which is `'static` and cannot borrow the application, so owned shareable handles are what let that call site take a capability instead of constructing one. **One capability is deliberately not in the struct, and the reason is iced's.** `OsThemeProbe`'s only consumer is `os_theme_poll`'s `Subscription::map` closure, and iced panics on boot if a subscription's mapping closure captures anything — a capturing closure has no stable identity, so the runtime restarts the timer every frame. That is recorded at `detect_system_scheme` as a bug already hit once. A closure that cannot capture cannot be handed a capability, so the probe stays chosen at its call site: a real exception to FR-018, recorded rather than forced, and invisible to the guard either way since that derivation reads `micold-core` and `SystemThemeProbe` is client-defined. **`StdFolderScanner` is built once and used twice.** T048's split left one production type implementing both folder capabilities; naming it twice would have given FR-018's guard two choices to count, so it is built into a local and shared — it is `Copy`, so this costs nothing and keeps "chosen in exactly one place" literally true. **`from_parts` was written and then deleted.** A constructor taking seven supplied implementations is the obvious next thing, and nothing called it — a seam nobody drives is not evidence that anything is substitutable, which is the argument T042 makes about a fake no test constructs. **What the capabilities bought is shown where it is real instead**: `prune_empty_sessions` decides at boot whether to drop a session, and it had **no test at all**, because `session_has_conversation` reached `ClaudeProvider` and the user's home directory — exercising it meant writing into `~/.claude`. Two tests now cover it through `FakeAiCliProvider`, including the uncertainty rule (a provider that cannot locate its config must keep every session, not prune them all). **Four probes, four distinct failure sets**: a second choice of one implementation inside the shell (FR-018), a feature module naming a real implementation (FR-017), uncertainty pruning instead of keeping, and pruning deciding nothing. **An operational note**: bare `cargo` in a worktree resolves to a worktree-*local* `target-shared/` rather than the shared one, so mixing it with `mise run` built everything twice and filled the disk mid-task; the 16G duplicate was removed and the probe script now exports `CARGO_TARGET_DIR`. Full workspace suite green at 194 binaries, clippy `-D warnings` clean, `cargo fmt --check` clean, assertion freeze "OK — 3789 intact, 2 added"
- [X] T050 [US3] Split `crates/micold-client/src/main.rs` startup into `crates/micold-client/src/shell/startup.rs` — `boot`, `window_settings`, `main` — with inline `#[cfg(test)]` tests relocated alongside (FR-019a, FR-027's relocation clause) — **`main` cannot move, so its body did.** Rust requires the entry point at the binary crate root; `main.rs` keeps a two-line `fn main()` calling `shell::startup::run()`, and everything the task means by `main` — the probe notice, the font registration, the window settings, the runtime handshake — is in the shell. `ICON_RGBA` moved with `window_settings`, its only reader, and its `include_bytes!` path gained a directory. **Nothing was relocated alongside, because there was nothing to relocate**: none of the three had an inline test. That is not an oversight in the original code but its symptom — `boot` reads the user's real data directory and opens a window. What made any of it testable was T049, and the first tests of what `boot` does landed there, against the provider port. Recorded rather than left as a silently unmet clause. **One existing guard had to follow the code, and its own text said so.** `tests/frame_probe_glue.rs` reads `src/main.rs` as *text* to check that the frame-time probe cannot leak into an ordinary launch, and `probe_config().map(` moved to `shell/startup.rs`, so it failed. Its vacuity test already carried the instruction — "if the binary moved, this file's path must move with it, or the gate above goes unchecked" — so `source()` now reads `main.rs` **plus** `src/shell/`, with `main.rs` first and the shell files sorted, so the frame-clock gate's "line before it" reading stays a property of one file rather than of the join. No assertion changed — a first attempt reworded the size check's *failure message* and the freeze check caught it at 89% similarity as a rewrite, which FR-027 forbids as squarely as a deletion, so the original wording went back; two assertions were added, naming each half, because a `source()` that quietly stopped reading `src/shell/` would still be tens of thousands of bytes of `main.rs` and would still satisfy the size check. This is the shape every remaining shell task will hit: a text-scanning guard pointed at `main.rs`. **Behaviour-neutral** — the extraction compiled with a single unused import as its only complaint, because `boot` already took everything it needed from `Capabilities` after T049. `main.rs` 3813 → 3685 lines. **Three probes, three distinct failure sets**: an ungated probe planted in its *new* home (caught, which is the point of moving the scan), the scan stopping at `main.rs` (the new vacuity assertion plus the moved gate), and an ungated frame clock still in `main.rs` (unaffected by the join). Full workspace suite green at 194 binaries, clippy `-D warnings` clean, `cargo fmt --check` clean, assertion freeze "OK — all 3791 intact"
- [X] T051 [US3] Split persistence into `crates/micold-client/src/shell/persist.rs` — `persist`, `persist_settings`, `prune_empty_sessions` — with its tests — **the task names three functions and there are two, and the missing one is the design rather than an omission.** `persist` does not exist in the client: the daemon's `Catalog` is the single writer of `projects.json`, because `store.rs` has no locking and a client-side save clobbers whatever the daemon wrote since this process loaded — with a copy in which `mode` means something different, which was the "worktree session starts with a plain terminal" bug. The comment recording all that moved here from `main.rs`, because it explains *this* module's boundary: what the client persists, and what it deliberately does not. **Two functions came along and one deliberately did not.** `session_has_conversation` moved with `prune_empty_sessions`, its only caller — the two are one rule about what a restart may resume, and FR-001a asks that a function and what it operates on stay together. `session_cwd_for_location` stayed: pure path arithmetic with two further callers outside persistence, so filing it here would group it by who happens to use it rather than by the external system it addresses, which is the thing FR-019a is asking about. **Both T049 tests relocated with the code they test** — that is FR-027's relocation clause working as intended, and the first time in this feature it has had anything to carry, since T050 found no inline tests to move. An assertion that changes house is not an assertion removed, and the freeze check confirms it: 3791 intact. **Three functions widened to `pub` to cross the module boundary**, which in a binary crate reaches no further than `pub(crate)` would; the same widening T019/T021 recorded for the feature modules. **A probe fired nothing, and that was the finding.** `persist_settings` writes a whole `Settings` on a theme change, and the `..existing` spread is the only thing stopping that write from resetting the scrollback limit and the entire environment-include configuration to their defaults — two features' worth of user configuration lost on a click that says "theme". Replacing the spread with `..Settings::default()` failed no test. It has one now, plus one for the refused write (a read-only home directory must be reported, not swallowed) and one for the absent store (no data directory is a supported configuration, not an error to notify about). **`persist_settings` was narrowed from `&Capabilities` to the one capability it uses**, which is FR-016's argument applied to a shell function rather than a trait — and it is what made those tests possible without reinstating the `Capabilities` constructor T049 deleted for want of a caller. Behaviour-neutral otherwise: no call site changed except its path. `main.rs` 3685 → 3559 lines; `shell/` is now four modules. **Five probes, five distinct failure sets**: the two relocated pruning probes still fire on their tests under their new module path (which is the relocation proving itself), plus the settings spread, the swallowed write, and a complaint on an absent store. Full workspace suite green at 194 binaries, clippy `-D warnings` clean, assertion freeze intact
- [X] T052 [US3] Split daemon synchronisation into `crates/micold-client/src/shell/daemon_sync.rs` — `send_op`, `switch_daemon_attachment`, `reconcile_catalog`, `PendingOp` — with its tests — **four named, and two more had to come.** `wire_to_lifecycle` and `wire_to_worktree_status` moved with `reconcile_catalog`, their only caller: both translate a daemon wire enum and nothing else in the client has a reason to (FR-001a, the same argument T051 made for `session_has_conversation`). `PendingOp::describe` came with its type. **`App` is the argument here, and that is the plan rather than a shortcut.** `shell/mod.rs` said at T050 that these modules would operate on `App`, the binary's own type, and this is the first one that does. It is *not* what T051 did with `persist_settings`, and the difference is what each function needs: `persist_settings` needed one capability out of seven, so FR-016's narrowing bought two tests; `send_op` needs four fields spanning two concerns — outbox, correlation counter, in-flight table, notification queue — and spelling those out at eleven call sites would trade a readable call for a parameter list. It cost nothing in testability: `base_app()` already builds a neutral `App`, and `Outbox::new` is already `pub` so a test can hold both ends of a channel without a daemon (its own doc comment records why). **The fixtures were split by what they are of, not by who calls them**: `snapshot_with`, `summary` and `summary_at` build daemon wire types so they moved here and `main.rs`'s stamper-seeding tests import them back; `base_app` went the other way, since `App` is the binary's. Both directions are `pub(crate)` across two `#[cfg(test)]` modules, which is unusual enough to be worth naming. **A latent doc bug fell out of the move**: `/// The binary's application state …` sat above `PendingOp`'s own doc comment and so documented `PendingOp`, not `App` — clippy's `empty_line_after_doc_comments` found it the moment the enum left and the blank line was exposed. **Five functions gained tests that had none**, which is the substance of this task beyond the move: the disconnected notice naming *which* op was dropped, the correlation id being the same on the wire as in the table, and the three attachment-switch rules. **A probe fired nothing, and the test it convicted was mine.** `a_switch_while_disconnected_sends_nothing` asserted nothing reached a channel the app was never given — with `daemon == None` there is no send side, so it could not fail under any implementation. Replaced with the one thing that *can* differ: the switch says nothing to the **user** either, unlike `send_op`, which reports every op it drops — deliberate, because a switch happens on every sidebar click and `DaemonConnected` redoes the attach anyway. Its probe has to change `&App` to `&mut App` to notify at all, which every call site accepts without complaint, so the signature is a habit rather than a wall — worth knowing. **Behaviour-neutral**: no call site changed except its path. `main.rs` 3559 → 3152 lines; `shell/` is now five modules. **Seven probes, seven distinct failure sets**: a generic disconnect notice, an op recorded under the wrong id, never detaching, always detaching, a switch that pesters the user, and the two relocated reconcile rules still firing under their new module path. Full workspace suite green at 194 binaries, clippy `-D warnings` clean, `cargo fmt --check` clean, assertion freeze "OK — 3838 intact, 14 added"
- [X] T053 [US3] Split subscriptions into `crates/micold-client/src/shell/subscriptions.rs` — `subscription`, `cursor_move_events`, `window_focus_events`, `os_theme_poll` — with its tests — **four named, six moved**: `os_theme_poll_interval`, the two `listen_with` callbacks and the three interval constants came with the functions that read them (FR-001a, the same argument T051 and T052 made). **The external system here is not an effect the application performs, it is one it listens for** — the iced runtime and the OS events it carries — and `subscription` is re-read after every update, so its `if`s are live decisions rather than setup. That is what makes this module different in kind from the four before it. **`detect_system_scheme`, `SystemThemeProbe` and `map_system_scheme` deliberately stayed in `main.rs` for T054**: they are the OS-theme *probe*, and the split follows FR-019a's line exactly — the runtime schedules, the probe asks the operating system — so `os_theme_poll` imports the one function it calls and leaves the rest. **Nothing was widened.** T051 and T052 both had to make functions `pub` to cross the module boundary; here only `subscription` is, because items at a binary's crate root are already visible to every descendant module. A first attempt widened `probe_config` and `detect_system_scheme` anyway and clippy caught it — `result_unit_err` on a now-public `Result<_, ()>` — which is how the unnecessary widening got noticed rather than shipped. **The gap this closed is the substance.** Three of the six subscriptions are conditional and each condition is the only thing between the idle window and a process that never sleeps (FR-025, FR-032a, SC-017); only the frame clock had a guard, in `tests/frame_probe_glue.rs`. The snackbar's 250 ms clock and the pointer stream had **nothing** — dropping either `if` compiles, changes no behaviour any test can observe, and leaves the application waking four (or sixty) times a second forever. `tests/idle_subscriptions.rs` covers them, structurally, because a `Subscription` cannot be inspected for what it contains. **It reads structure rather than the preceding line, and that difference is forced**: `frame_probe_glue` asserts the nearest line of code above the frame clock opens a `probe_config()` test, which is exact but only survives while that push stays a one-liner — the snackbar's spans four, so the same rule would read `subs.push(` and call it unguarded. A brace-depth walk naming the enclosing block costs thirty lines and does not care how the argument is wrapped. **The file carries two controls of its own**: the frame clock is asserted a second time by the walk, so a disagreement with `frame_probe_glue`'s independent reading convicts one of them; and the theme poll is asserted to be **un**conditional, which is both the 003 FR-006 regression (a poll suspended on blur left a visible window showing the wrong theme, and leaving the app to change the theme is what unfocuses it) and a vacuity check that a walk reporting "inside an `if`" for everything would fail. **A third orphaned doc comment fell out of the move** — `window_focus_events`'s doc had drifted above `cursor_move_events`'s own, documenting the wrong function and leaving the right one with none; T052 found the same shape on `App`/`PendingOp`. **Behaviour-neutral**: no call site changed except `startup.rs`'s `.subscription(...)` path. `main.rs` 3152 → 2913 lines; `shell/` is now six modules. **Ten probes, ten distinct failure sets** — an ungated snackbar tick, an ungated pointer, a theme poll made conditional, a background cadence past SC-003's second, the two cadences swapped, every mouse event mapped to a cursor move, an inverted focus mapping, an ungated frame clock (which fires **both** frame-clock guards, in two files, which is the cross-check working), the walk reporting the outermost block instead of the innermost, and comments no longer stripped. **A probe fired nothing, and the finding is that the code is redundant rather than the test weak**: removing `.max(0.0)` from the cursor coordinates failed nothing, because Rust's float→int `as` cast already saturates — `-5.0f32 as u16` *is* `0`. That assertion pins a language guarantee, not this function. The clamp stays as documentation, and a replacement probe (every mouse event becoming a `CursorMoved`) shows the mapping test is not vacuous. **Two probes of my own harness were wrong before the code was.** `cargo test` stops at the first failing binary, so the first pass judged four probes by a truncated set — `--no-fail-fast` is not optional when the question is *which* tests a mutation moves. And the build-error classifier read cargo's ordinary `error: test failed` / `error: N targets failed:` as a compile failure, marking every probe a build error for a whole pass; it now matches build failures explicitly and saves each run's raw output so a misparse cannot cost another pass. A rejected probe is recorded too: dropping the walk's pop loop collides with the theme-poll probe, because the planted fixture nests with parentheses rather than braces, so a stack that never pops still answers correctly for it. **An operational repeat of T049's trap**: the probe script's bare `cargo` resolved `target-dir` against the *worktree's* checked-in `.cargo/config.toml` and built a second full copy of the tree; it now exports `CARGO_TARGET_DIR`. Full workspace suite green at 196 suites, clippy `-D warnings` clean, `cargo fmt --check` clean, assertion freeze "OK — 3832 intact, 26 added". **CI needed three attempts, and not because of this change**: `crates/micold-core/tests/env_include_resolve.rs`'s Windows tests failed with `TimedOut` on the first, hung for two hours in the same step on the second, and passed on the third. The same failure is on `main` at 2751fa9. Intermittent, not caused here, and it will be in T077's way
- [X] T054 [US3] Split the remaining two systems into `crates/micold-client/src/shell/env_include.rs` and `crates/micold-client/src/shell/os_theme.rs` with their tests — **the shell split's per-system decomposition is complete at seven modules.** `env_include.rs`'s external system is **a subprocess running the user's own script** — the widest blast radius in the client, arbitrary code for an arbitrary duration producing arbitrary output — and after T046 what remains here is the shell's two jobs: picking the real resolver (FR-017) and knowing *which* directory and *when*. `os_theme.rs`'s is the desktop environment's light/dark preference, the codebase's only direct OS branch, which is why Principle VI comes down to one file. **`default_resolution_cwd` came along, and the task's wording is why**: it names two *systems*, not two functions, and all five of its call sites pair with an env-include resolve or refresh — it exists for the moments where no particular session is being asked about and one representative directory is needed anyway (boot, a Settings save). Filing it elsewhere would group it by who calls it rather than by the system it serves (FR-001a, the same argument T051 made for `session_has_conversation` and T052 for the wire translators). **The theme is deliberately split across three places, and none of them is a compromise.** `subscriptions` owns the clock that decides when to ask (T053), `os_theme` owns the asking, and the fallback for a failed probe is in neither — deciding what to do about `Err` is not an I/O concern, it is `theme::observe_system_scheme`, pure and tested in the core (FR-016). Three different things that meet at one message. **`detect_system_scheme` is `pub(crate)` rather than `pub`, and the reason is a lint rather than a design**: in a binary the two reach exactly as far, but clippy's `result_unit_err` fires on an exported `Result<_, ()>`. T053 hit this and dodged it by not widening at all; here the move forces a widening, so the narrower one is the answer. The `()` is `OsThemeProbe::detect`'s own signature — "the OS declined to say", carrying nothing because there is nothing to carry — and widening the error type to satisfy a lint would have been the wrong fix. **Nine functions moved and none of them had a single test; there are eleven now**, which is again the substance beyond the move: that the user's script path, directory and timeout all reach the subprocess unaltered (the directory is what BUG-002 was about — a resolver handed the wrong `cwd` returns a snapshot that looks perfectly healthy and is simply another directory's answer); that a disabled setting and a blank path each spawn nothing *at all*, not merely discard the result; that a refresh writes the cache entry and the reported outcome from one snapshot, so the terminal launch and the Settings pane cannot disagree about what just happened; that refreshing one directory leaves every other directory's answer alone; and the two fallback ranks of the representative directory. **`refresh_env_include` is tested against the real capability, on purpose.** It takes `&mut App`, so it reads the resolver out of `Capabilities::real()` and cannot be handed a fake — T049 deleted `from_parts` for want of a caller and this is the first place one would have helped. Rather than reinstate a seam for one test, the bookkeeping tests set `env_include_enabled = false`, which short-circuits in the core before the resolver is consulted: the same code path, no subprocess, no new construction API. The resolver's own contract is covered separately through `FakeEnvIncludeResolver` on `resolve_env_include`, which does take a `&dyn`. **Ten probes: nine distinct failure sets, and the tenth is a recorded null.** A timeout read as milliseconds, a disabled setting that still runs the script, a blank path sourced anyway, a refresh that records the outcome but not the cache entry, a refresh that discards every other directory, the process directory outranking the open project, no directory at all reported as an empty path, an unspecified OS preference reading as dark, and a probe that panics instead of answering — that last one also fails `update_inner_applies_window_focus_changed`, which turns out to reach the OS through `App`'s construction. **The null is by construction and was predicted before it ran**: a probe that always returns `Err` fires nothing, because `the_probe_answers_without_panicking_whatever_the_desktop_says` tolerates `Err` deliberately — a headless runner has no preference to report and a developer's machine has whichever one they set, so what the probe *answers* is not assertable anywhere. It still catches a panic, which is what P9 shows. An earlier draft did try to assert something stronger — that `detect_system_scheme()` equals `SystemThemeProbe.detect()` — and it was removed before it ran: two live calls to a backend with a 25 ms D-Bus timeout is a flaky test, and this feature had just spent an afternoon on someone else's. **Behaviour-neutral**: no call site changed except its path. `main.rs` 2913 → 2803 lines. **A `python3` whole-file rewrite of `main.rs` truncated it to zero bytes when the disk hit 100%** — restored from HEAD, and the edit redone with targeted replacements instead. The cause was 44G of stray worktree-local `target-shared/` directories, T049's trap again: `.cargo/config.toml` resolves `target-dir` against the *worktree's* copy of itself, so a bare `cargo` never shares the directory `mise run` uses. Full workspace suite green at 197 suites, clippy `-D warnings` clean, `cargo fmt --check` clean, assertion freeze intact
- [X] T055 [US3] Move `update_inner`'s effectful arms from `crates/micold-client/src/main.rs:775–2028` to the shell module addressing each arm's external system — **`update_inner` is now dispatch, and the 50 arms are unchanged.** Every arm was lifted *verbatim* by a script that finds the arm's opening line, walks braces to its match, and dedents — because the point of this task is that an arm addressing an external system lives with that system, not that its logic changes, and a hand-transcribed body could drift in ways no diff review would catch. `main.rs` 3007 → 1666 lines. **Two shell modules the T050–T054 split did not produce, and their absence was the finding.** Those tasks moved out every system that had a *function* in `main.rs` named after it; two systems never did, existing only as arms, so there was nothing to move until the arms moved. `shell/workspace.rs` is **the user's filesystem and their git working copies** — browsing for a folder, deciding whether it is a repository, enumerating its worktrees. `FolderBrowser` and `Git` stay separate as *capabilities* but are one *module*, because they are one conversation: the picker exists to find a repository, `FolderChosen` asks git whether the chosen directory is one, and the same arm then asks git what worktrees it holds. FR-019a asks what a change to one external system can reach, and a change to "how we open a project" reaches both; splitting it would put two halves of one decision in two files. `shell/service_control.rs` is **the session service as an OS process**, and it is deliberately not `daemon_sync`: that module is the conversation with a daemon that is running and speaking the protocol, this one is what you do when it is not. Terminating a mismatched build by pid is used precisely *because* the protocol is unusable (FR-022), and logout survival is `loginctl`/`systemctl`, which the daemon does not speak at all. Same argument T054 made for splitting the OS-theme probe from the clock that drives it — two systems that meet at one message are still two systems. **`shell/clipboard.rs` took `interpret` and `selection_copy_request`, and the guard followed the code because its own failure message said to.** `tests/clipboard_request.rs` pointed at `main.rs` in two places, one of them carrying the instruction verbatim: "either it moved to `shell/` — update both tests together". Both now read `shell/clipboard.rs`. Relaxing them was never an option — an ungated clipboard write is exactly as invisible from `shell/` as from `main.rs`, which is the same argument T050 recorded for `frame_probe_glue`. **Eight helpers moved with the arms that were their only callers** — `view_and_start`, `send_pane_size`, `attach_current_process`, `start_resolved_create`, `send_worktree_create`, `session_process`, `describe_leftovers`, `worktree_create_error_text` (FR-001a) — and the last one's two tests came along under FR-027's relocation clause. **The OS-theme re-detect left `WindowFocusChanged` and the comment did not.** `redetect_on_focus` is now `shell/os_theme.rs`'s; what stays in the arm is the paragraph explaining why it must never touch terminal focus (feature 023, FR-013–FR-015), because that is a fact about the dispatcher rather than about the theme. Twenty-seven other comment blocks moved *to* the functions they now document, by script — a comment that stays behind when its code leaves is the orphaned-doc bug this feature has now hit four times. **Nine functions across the two new modules had no tests; there are ten now**, including the two the probes forced apart. **Ten probes, ten distinct failure sets after a split.** A folder gate that admits anything, a refusal reported behind the modal's scrim, a listing that ignores the directory it was asked about, an unreadable directory reported as an empty listing, a restart that leaves the banner it acted on, a restart that says nothing about stopping processes, a swallowed survival outcome, a clipboard translation that grew a decision, a clipboard that writes nothing, and a project switch that never releases what it leaves. **Two of them initially failed the same test, and splitting it was the fix rather than dropping a probe**: `a_folder_that_is_not_a_repository_is_refused` asserted both that the project is refused *and* that the picker closes first. They are separate bugs — closing the picker late still refuses the project correctly and simply renders the notification behind the scrim, so the user sees nothing happen and clicks again. One assertion each, and the probes now separate. Full workspace suite green at 197 suites, `cargo fmt --check` clean, clippy `-D warnings` clean workspace-wide, assertion freeze "OK — 3900 intact, 8 added". CI green on all three platforms first try, including the Windows env-include tests that needed three attempts at T053 and two at T054
- [X] T056 [US3] Route clipboard through `Outcome::ClipboardWrite` interpreted by the shell, replacing the three direct `iced::clipboard` calls at `crates/micold-client/src/main.rs:1840, 1847, 1856` (FR-015a) — **done with T045, which could not compile without it** (recorded there in full). Two of the three convert: the paste's `clipboard::read` stays a shell call, because the contract has reads arriving back as an ordinary message exactly as today and no feature requests one. `interpret` is the shell's whole translation — one arm per variant, no branch — and `the_shells_translation_decides_nothing` reads its body to keep it that way, because a body that grew an `if` would still compile and still pass every behaviour test. Behaviour-neutral: the same two decisions, made in the module that owns the selection instead of the line above the write. Ordered before the shell split rather than after it — T050–T055 move `update_inner`'s arms into `shell/`, and an arm that already interprets a request moves the same as one that does not
- [X] T057 [US3] Write the "adding a capability" section of `docs/development/architecture.md` — **five steps, and the section's job is to make the boring ones boring.** Declare the trait in the core beside the domain it serves (not a `ports/` directory, the same argument Tier 1 made for feature types); write the fake next to it; add the port name to `PORTS`; choose the real implementation once in `Capabilities::real`; use it from the shell module for its external system. Each step names the guard that holds it, because every one of these rules is checked rather than trusted and a developer who knows *which* test will fail has been told something a prohibition does not tell them. **The width rule is quoted from the contract rather than paraphrased**, and the record of how it was answered is the substance: it fired six times, `FolderScanner` was genuinely too wide and was split, `ProjectStore` was not and its stubs were a missing shared fake showing up as a width symptom. "A width failure is a question, not a verdict — but the check is never the thing that gets relaxed" is the sentence the section exists to be able to say. **Three exceptions documented rather than smoothed over.** `OsThemeProbe` is chosen at its call site because iced forbids a capturing subscription closure, and it is invisible to the FR-018 guard regardless since its real implementation lives in the client; `TerminalBackend` and `TerminalHandle` have only fakes in the workspace, so there is nothing for an assembly point to choose. Counting them is what makes "seven of ten ports are in `Capabilities`" a fact a reader can check rather than a discrepancy they trip over. **And the clipboard gets its own subsection**, because it is the one I/O concern that is deliberately *not* a capability (FR-015a) — a reader following steps 1–5 for something whose call sites return a `Task` would otherwise write a port that cannot exist. **Two stale facts elsewhere in the document, both created by T055 and both fixed here rather than left for a later reader**: the Settings-form exception pointed at `main.rs`'s `SettingsSaved` arm, which is now `shell/persist.rs::on_settings_saved`, and the Tier table still called the shell split pending. A third was in the code — `shell/capabilities.rs`'s own doc cited `main.rs`'s `boot_drops_a_session_the_provider_has_no_conversation_for`, which moved to `shell/persist.rs` with T055's arms. That is the orphaned-reference bug this feature keeps hitting, in its documentation form: the comment stayed put when the code left. Docs-only apart from that one comment; `cargo fmt --check` clean, and nothing in the suite or CI reads this file, which is stated rather than assumed

**Checkpoint — SC-004b**: Tiers 1 and 2 and the shell split are all merged with **zero Tier 3 work**.
Demonstrating green here is the criterion. Do not start Phase 6 before recording it.

- [X] T058 [US3] Record the SC-004b demonstration: full suite green on all three platforms with no part of Tier 3 merged, noted in `specs/021-mvu-slice-architecture/baseline.md` — **recorded at `e91d468`, CI run 32175454220, all eight checks green.** **The task's own wording overstates what CI demonstrates, and that is the finding.** "Full suite green on all three platforms" is not what the matrix does: `ci.yml`'s "Test (full workspace)" step carries `if: runner.os == 'Linux'`, so macOS and Windows run the render-free core, a workspace *build*, and the eleven window-free client gates — 72 suites / ~707 tests against Linux's 272 / 2,625. What is demonstrable is *the full suite green on Linux, and everything that can run without a window green on all three*, which satisfies SC-004b (whose subject is the tiers, and every guard this feature added is in the Linux run) but **cannot satisfy SC-006**, whose wording is "the complete pre-existing test suite passes … on all three supported platforms". **T077 inherits a decision, not a discovery**: either run the client suite on macOS and Windows (a CI change, not a code change) or restate SC-006 against what the matrix covers. **Zero Tier 3 was checked step by step rather than asserted** — no `reducers/` module and both reducers still single functions (step 17); no `Message` enum under `features/` and the form's 14 variants still in the root enum (step 18); no FR-024a/SC-007 guard (step 20 — both identifiers *do* occur in `crates/`, belonging to features 017 and 018, which is why the check had to be scoped to this feature's numbering rather than the string). **Step 19 is partial and the exception survives inspection**: `Outcome` holds exactly one variant, `ClipboardWrite`, with no root interpreter, no reducer split and no feature learning about another — FR-015a's shell-interpreted borrowing of FR-021's vocabulary, exactly as T045 argued. **Two counts moved the wrong way and both are expected**: `State::update` 778 → **834** because Phase 5 pushed `update_inner`'s pure arms down into it while its effectful arms went to `shell/` (which is why one reducer fell 85% to 185 lines and the other did not fall at all) — that 834-line function *is* step 17's subject; and `Message` 130 → 137 because other features keep landing on `main`. `main.rs` 3,567 → **1,672** total, 2,715 → **861** production (−68%). `Overlay` and `ClosingOverlay` are deleted outright, 19 variants gone. **SC-003 is not met here and was not due to be**: `app.rs` (1,872) is still second-largest, and it will not drop until step 17 takes the reducer out; the new largest file in the repository is `shell/daemon_sync.rs` at 1,960 (1,517 production), which is the shell split concentrating one external system — FR-005 governs, but it is recorded rather than assumed. **Two known load-sensitive tests are logged against T077 rather than left to surprise it**: the env-include Windows flake (BUG-004, fixed one commit before this checkpoint — but the Windows job's five-run green streak includes three runs *from before* the fix, so it is not evidence the fix worked), and `micold-core/tests/typeahead_budget.rs` (BUG-003), which has never failed in CI but is a *core* test and therefore runs on all three matrix jobs, not just Linux.

---

## Phase 6: User Story 2 (part 2) + User Story 4 — Reducer split and outcomes (Priority: P1/P2)

**Goal**: The reducer becomes per-feature modules over the shared state, and cross-feature effects
become explicit returned outcomes. research.md §6, steps 17–20.

**Independent Test (US2)**: The root state, messages and reducer contain composition and routing
only — no feature's decision logic (FR-002).
**Independent Test (US4)**: Run the worktree-delete path in isolation; assert it returns outcomes
describing the session and overlay consequences while touching only worktree data. The existing
`worktree_delete.rs` must pass unchanged.

**Applies to both reducers.** Per FR-004a as amended, a feature's pure arms go to its reducer module
and its effectful arms to the shell module for their external system. `app.rs::update` is 778 lines;
`main.rs::update_inner` was 1,253 before Phase 5 reduced it.

### Tests for Users Stories 2 and 4 — write first, observe failing ⚠️

- [X] T059 [P] [US4] Cross-feature write guard in `crates/micold-client/tests/feature_write_isolation.rs` asserting no feature reducer mutates another feature's data, and **naming the offending path** in its failure message (FR-020, FR-024a, SC-007, contract O6) — **landed at `f9fafed`, with the `impl_blocks` fix at `5ff0ef0`.** **Ownership had to be a map of paths, not of fields.** `State` has 41 fields but one of them, `workspace`, holds the project catalog, the session lists and two worktree maps in a single value; a field-level map would have to assign it to one feature and would then be wrong for the other two. `OWNERS` is therefore keyed by path and `workspace` appears only through its six members. **`Workspace`'s own methods are resolved the same way, and had to be.** The first run's unclassified-method check — which fails rather than guessing — reported `workspace.forget` among others; it writes **five members across three features**, which no single mutates/reads verdict could express. Its methods are now resolved to the members they actually write, and a call site inherits that set. **Indirection is followed to a fixed point** because that is where the writes hide: `restore_after_activation` → `arm_notice` → `push_notification` → `notify`. A scan stopping at the first call reports the session feature clean while it raises notifications. **Each write is then reported once, at the innermost feature operation that reaches it** — the first version reported 18 violations where there are 8, because `switch_active` and `restore_after_activation` inherit what `set_current_session` writes. An allowlist naming callers that converting the real write would silently fix is precisely backwards for T067a. **Eight cross-feature writes exist today and are seeded into `ALLOWED`**, which is what T067 transcribes and T067a burns down; `the_allowlist_names_only_live_violations` fails if an entry stops being a violation, so a conversion that forgets to delete its line is caught by the test that permitted it. **One of the eight may not be a violation at all, and T067 has to decide rather than this guard**: `restore_after_activation` writes `focused_field`, which is `root`-owned rather than another feature's, and it gets there through `State::focus_terminal` in `app.rs` — either root state is writable by anyone or `focus_terminal` is a session operation in the wrong file. One other already has its answer: `arm_notice` → `notify` is the contract's own `NotificationRaised`. **Landing red was not an option** (SC-009), which is why the allowlist exists at all; the discovery mechanism the task list already describes at T067 is what it feeds. **Six probes, six distinct failure sets**: a feature writing another's data (the rule), an unclassified method (the classification test), an allowlisted violation ceasing to be one (rule + liveness), an unowned `State` field, a stale `OWNERS` workspace entry, and the `impl` scan matching no type (liveness + vacuity). **The classification test is separate because a probe collision forced it** — an unclassified method and a planted write both failed the rule and nothing else, so no failure set could tell them apart. T055 hit that shape, BUG-004 hit it again, this is the third. **Two probes fired nothing on the first run and the finding was a hole in the guard, not weak probes**: `impl_blocks` matched the literal `impl State {`, so a module writing `impl crate::app::State { … }` — the form a new feature module is likeliest to write, having no `use` for it yet — was invisible to the entire scan. Fixed to match on the last path segment, and to accept trait impls. **A third probe was bad rather than informative**: adding a field to `Workspace` breaks unrelated construction sites, so it reported a build error instead of a failure set; replaced with one that adds a nonexistent member to `OWNERS`. **A fourth was stale**: after the `impl_blocks` rewrite its needle no longer existed, so it patched nothing and fired nothing — a no-op probe reads exactly like a hole, which is why each probe now asserts its needle is present before mutating. Guard tests 6, `cargo fmt --check` clean, workspace clippy `-D warnings` clean
- [X] T060 [P] [US4] Termination test in `crates/micold-client/tests/outcome_termination.rs` asserting outcome interpretation terminates under a cycle and does not depend on composition order (FR-024, contract O4/O5) — **landed at `5ff0ef0`, paired with the drain it tests.** **The drain arrived here rather than at T065 for the reason T045 recorded**: a test naming a function that does not exist is a compile error, not a red test. **T065's text needs a second edit** — it reads "and the root's draining interpreter", which is now built; what remains there is wiring the three new variants into it. **Generic over the apply step, and that is the point.** No real interpretation can produce a cycle today, so driving real variants through the loop could never observe the bound being reached at all — a guard exercisable only by behaviour that does not exist yet is a guard nobody has run. The tests hand it cascades real code cannot make. **FIFO is the whole of O5** and one test holds it: a stack would run one feature's cascade ahead of an outcome another feature had already emitted, making the interleaving depend on composition position. **A test I wrote turned out weaker than its name, and the probe is what showed it.** `each_features_own_order_survives_either_composition` does **not** distinguish a queue from a stack: under LIFO both compositions reverse symmetrically, so each feature's own outcomes keep their relative order and it passes. P7 confirmed a prediction made in writing before the run. `a_cascade_does_not_preempt_another_features_pending_outcome` is the only test that separates them, which is what its doc comment already claimed. **The bound's range is a `const` assertion, not a test.** clippy's `assertions_on_constants` is right that an `assert!` over a constant is a compile-time fact; the consequence, recorded rather than discovered later, is that a probe against the bound's range reports a **build error** rather than a failure set — the same signal a bad probe gives, so the two must not be read alike. **Three probes, three distinct sets**: queue → stack (cascade + emission order), the bound stopping without asserting (the cycle test), and newly emitted outcomes jumping the queue (cascade). **The second of those exposed a bug in my own probe harness before it exposed anything about the code**: the failed-test regex could not parse `test … - should panic … FAILED`, so a probe that fired correctly was recorded as firing nothing. Third harness fault of this feature, after T053's fail-fast and build-error misreads. **And the probe script cost a redo of this task's implementation**: its `git checkout -- .` reverted the drain out of `app.rs` between writing and verifying. The saved rule is "commit before probing"; what it does not say, and now should, is that the restore only touches **tracked** files — `outcome_termination.rs` survived untracked while `app.rs` did not, which made total loss look selective. 12 tests green across both guard binaries, clippy `-D warnings` clean
- [X] T061 [P] [US2] Root-routing test in `crates/micold-client/tests/root_is_routing_only.rs` asserting the root reducer contains no feature decision logic (FR-002) — **landed at `ea864bd`. 110 arms: 17 route, 93 decide.** **Routing means the arm hands the message on without answering anything itself** — an empty body, or calls, and nothing else; deciding means control flow, a comparison or boolean operator, or an assignment into the state. Those are the shapes a feature's rule takes. **`ROOT_DECISION_ARMS` is an exact count, not a ceiling.** Landing red is not an option under SC-009, and "no more than today" would let the number sit where it is forever; T062 and T063 lower it to zero. Exact also catches the direction FR-002 is really about — a feature adding a rule to the root fails the day it lands rather than at a review months later. **Under Tier 1 a feature's operations are `impl State` methods in its own module**, so `self.set_current_session(id)` already counts as routing: the decision is in `features/session.rs`. The classifier does not distinguish that from a future call into a reducer module and deliberately does not try — both are a call, and the question is what the *root* still answers. **Recorded limits**: a decision hidden inside a call is invisible here wherever the callee lives, including `app.rs` itself (`feature_write_isolation.rs` is what watches where those bodies write); and `update_inner` is out of scope, Phase 5 having split the effectful arms by external system. **Two probes, two distinct sets**: the classifier matching no arm (arm-scan vacuity + the count) and a routing arm gaining a decision (the count alone). **The second probe was wrong before the code was.** It first targeted `ProjectSelectorClosed`, which already counts as deciding because it assigns `self.selector = None`, so adding an `if` could not move the count and it fired nothing. A probe has to move an arm *across* the boundary, not deeper into the side it is already on; retargeted at `WorktreeExpansionToggled`, one of the 17 routing arms, it fires the count. 2 tests green, clippy `-D warnings` clean

### Implementation

- [X] T062 [US2] Split `State::update` at `crates/micold-client/src/app.rs:1165–1942` into per-feature reducer modules, one per feature module from Phase 3, each operating on the shared state (FR-004a) — **landed at `e1f6167`. 780 lines of reducer became 99 lines of routing; 90 arms moved into 8 modules** (help 3, settings 11, sidebar 7, project 16, session 17, worktree 16, worktree_form 22). **A reducer module is the feature module, not a sibling of it.** FR-001a forbids separating a type from the functions over it by a module boundary, so a `reducers/` tree mirroring `features/` would be the split the requirement exists to prevent; each arm became a free function taking `&mut State` beside the types and surfaces its feature already held. **`root_is_routing_only.rs` falls 93 → 3**, and the 3 are the floor rather than a stall: `FieldFocusChanged`, `CursorMoved` and `WindowResized` write `focused_field`, `cursor` and `window_size`, the fields `feature_write_isolation.rs` attributes to `root` because no feature owns them — window facts every feature reads and none houses. Whether a *window* feature should exist is T063's call, not T062's, so the test's header now argues the floor instead of promising 0. `dismiss_on_scroll_beneath` widened to `pub(crate)` exactly as T023 predicted; `SystemThemeChanged` went to `settings`, `system_scheme` being settings-owned. **Two defects in T059's guard, both invisible until free functions existed, and both failing in the direction that reads as success.** (1) `mut_param` compared `"&mutState".split("::").last()` against `"State"` — it never matched a free function, so all 90 new reducers were invisible and the guard reported every feature clean, while its own header promised it would pick them up. The reference is now peeled, lifetime included. (2) Operations were keyed by bare name, which was safe while they were uniquely-named `impl State` methods; T062 gives five modules a `menu_toggled` each plus repeated `opened`, `cancelled` and `rename_started`, so each name became one entry holding the union of every body's writes. The guard then accused each of writing the others' fields — **69 violations, 34 false**, and symmetric, which is what gave it away: `settings::opened` writing `worktree_form`, `worktree_form::opened` writing `settings_draft`. Now keyed by `file::name`. The vacuity floor `feature_ops >= 8` sat happily above a number that had not moved while ninety operations arrived unseen; raised to 100. **`ALLOWED` goes 8 → 43 with no behaviour change**: the 8 were never the violations in the codebase, only those in the code that had already moved, since the scan reads `src/features/` alone. The 35 added were arms of `State::update` all along. They are grouped by *what performs the write*, because most are not a feature reaching into a neighbour — they are a root helper writing across features, attributed to whichever feature called it (`clear_for_dialog`, `focus_terminal`, `set_worktrees`, `push_notification`, `Workspace::forget`), plus the popover mutual-exclusion rule, one fact about the toolbar no feature owns. That grouping is what T067 needs: an outcome per group, not one per line. **`current_session_writers.rs` follows its exemption to the new home** — counting writes in `app.rs` says nothing now (there are none) and counting them in `features/session.rs` would say 2 without saying which 2, so it names the enclosing functions, `set_current_session` and `selected`; `app.rs` left `EXEMPT`, putting the root back under the first test rather than holding a permission nothing uses. 104 client suites green, workspace clippy `-D warnings` clean. **Three probes, three disjoint single-test sets.** (P1) A feature's rule moved back into the root — `SidebarToggled` inlined as `self.sidebar_hidden = !self.sidebar_hidden` — fires `the_root_reducer_decides_no_more_than_it_did` and nothing else. (P2) A feature reaching into a neighbour with no allowlist entry — `sidebar::filters_cleared` also writing `about_open` — fires `no_feature_writes_another_features_data` and nothing else; that it fires *at all* is the check that the `mut_param` fix took, since the same probe against the pre-fix guard would have been silent. (P3) A moved arm losing a step — `session::selected` keeping its `active_session` write but skipping `focus_terminal` — fires `every_navigation_to_a_terminal_clears_a_release` and **none of the structural guards**, which is the point: the shape stays perfectly legal, so only the assertion freeze can see it, and it still can after the move. **One harness fault and one process fault, no code fault.** The probe script classified a run as `BUILD-ERROR` on `^error`, which matches cargo's `error: test failed` — the line it prints when a probe *works*; P1 and P2 were labelled build errors with no compile error in their logs, and the matcher is now `could not compile\|error[E`. And this record was written before probing rather than after, so the probes' own `git checkout -- .` reverted it — the saved rule says probe scripts restore tracked files, and `tasks.md` is tracked. **Record after probing, not before**
- [X] T063 [US2] Reduce the root reducer in `crates/micold-client/src/app.rs` to routing only, dispatching to the per-feature reducer modules (FR-002) — **landed at `31b5f0b`. `ROOT_DECISION_ARMS` 3 → 0.** T062 did the dispatch and stopped at three arms; this task was the decision they were waiting on, and the decision was to create **`features/window.rs`**. **The argument.** FR-002 has two clauses that disagreed here: "composition and routing only" said the three arms violate it, "MUST NOT contain any individual feature's decision logic" said they do not, since no feature owned `focused_field`, `cursor` or `window_size`. What resolves it is that *a field the root still decides about is a feature nobody has named* — and FR-001 has the precedent in this very feature: T031 created `features/help.rs` because the overflow menu had no home either, on the reasoning that FR-001 asks where a feature lives, not how big it is. The three belong together because each answers "what is the window doing right now", each is transient, and each is reported by the windowing system rather than chosen by the user. **`FieldId` moved too, and was overdue.** It was still a custom type in `app.rs`, in plain breach of FR-001's "every custom type moves into a module named for that type or the feature it serves" — the eight importers named the file it happened to be declared in rather than its home. That it survived Tiers 1 and 2 unnoticed is worth recording: the tier tests count *modules* and *arms*, and nothing counted types left behind. **Cross-feature reads are not the issue and never were.** Every feature reads these three — a context menu anchors on `cursor` and clamps on `window_size`, every text field's focus chrome comes from `focused_field` — and FR-003a permits that explicitly, isolation being enforced on writes. **What it settles for T067.** `OWNERS` gains `window` and loses `root` entirely. The 14 `focused_field` entries in `ALLOWED` were the ones T059 recorded as unanswerable — "is writing state nobody owns a violation at all?" has no answer while the owner is nobody. They are now ordinary cross-feature writes with a named owner, so T067 can propose the outcome it could not propose before (something like `DialogOpened`, applied by the root to the window feature). T059's other half — whether `focus_terminal` is a session operation in the wrong file — is untouched and still T067's. **A guard doc needed amending for a reason worth stating**: the exact count can no longer catch a classifier that calls *everything* routing, because 0 is the answer such a classifier and a correct one both give. The arm total is what carries that direction now — a scan that has stopped parsing reports 0 arms, not 110. 104 client suites green, workspace clippy `-D warnings` clean. **Three probes, three disjoint single-test sets.** (Q1) A window rule inlined back into the root — `CursorMoved` assigning `self.cursor` — fires `the_root_reducer_decides_no_more_than_it_did` alone. (Q2) Another feature writing window-owned data — `sidebar::filters_cleared` also zeroing `window_size` — fires `no_feature_writes_another_features_data` alone, which is the check that `window` is a **live owner** in the guard rather than a relabelling that reads well and enforces nothing. (Q3) The moved focus rule losing its guard — the blur believed from any field, not only the one holding focus — fires `moving_between_two_fields_leaves_the_second_focused_whichever_order_the_reports_arrive` and no structural test, which is the point: the shape stays legal, and only the assertion freeze sees it. **One build error, caught by the compiler rather than by review**: `tests/app_state.rs` imported `FieldId` from `app` and was missed in the first sweep, because the sweep grepped `src/` and the tests import through `micold_client::app::`
- [X] T064 [US2] Promote the worktree-creation form to a nested unit in `crates/micold-client/src/features/worktree_form.rs` with its own message type absorbing the 22 root variants — 18 `AddWorktree*` and 4 `WorktreeCreate*` — routed through one wrapping variant (FR-003; the sole nested unit per research.md §5) — **landed at `c4b3e29`. 22 variants → `Message::WorktreeForm(worktree_form::Msg)`; 22 root arms → 1.** **Root arm count 110 → 89, and the arithmetic is the check**: 110 − 22 + 1 = 89 exactly, which is what says the collapse lost nothing — a scan that had merely stopped *seeing* some arms would not land on the number the change predicts. The vacuity floor in `root_is_routing_only.rs` fired at `>= 100` and was lowered to 85 with that reasoning recorded beside it. **The variants kept their meaning and lost their prefix** — `AddWorktreeTicketChanged` is `Msg::TicketChanged`, since the type now says which form. The four `WorktreeCreate*` joined them because the create is this wizard's last step, not a separate concern, which is why §5 counted 22 and not 18. **Seven types left `app.rs` with the variants that named them** (`ConventionalType`, `Direction`, `BranchSource`, `BranchCandidate`, `BranchSituation`, `CreateMode`, `CreateStage`): the vocabulary reduction showing up in the imports rather than only in the enum. **The shell split holds on the same boundary** (FR-004a): the four effectful arms in `update_inner` are now nested patterns on the same wrapper. **Two mechanical hazards, both caught by the compiler.** Six sites passed `Message::AddWorktreeTicketChanged` as a *function* (`Fn(String) -> Message`) to `on_input`/`Select::new`; wrapping a constructor is not composing it, so those needed `|t| Message::WorktreeForm(…(t))` closures. And `main.rs`/`shell/daemon_sync.rs` are the **binary** crate, where `crate::` does not reach the library — they take `micold_client::features::worktree_form::Msg`, aliased `FormMsg`. 104 client suites green, workspace clippy `-D warnings` clean. **Three probes. Two fire one test each; the third fires nothing, and that is the finding.** (R1b) The root arm decides but decides nothing behavioural — `if self.worktree_form.is_some() || self.worktree_form.is_none()` — fires `the_root_reducer_decides_no_more_than_it_did` alone. (R2) A nested variant silently dropped from the wizard's own reducer — `Msg::TicketChanged(_) => {}` — fires `form_edits_build_a_derived_preview` alone. **(R3b) Deleting the shell's `on_add_worktree_submitted` dispatch, so submitting the form never sends the daemon RPC, fires nothing at all across all 104 suites.** The cause is structural and pre-existing rather than anything T064 introduced: `update_inner` lives in `main.rs`, which is the **binary** crate, and the integration tests in `crates/micold-client/tests/` link the *library*. Nothing under `tests/` can call it — `frame_probe_glue.rs` only greps its source text. So FR-004a's "a feature's pure and effectful arms end on the same feature boundary" is now true of the code, but only the **pure** side is guarded by tests; the shell's dispatch table is reachable only from `main.rs`'s own `#[cfg(test)]` module. Worth a task of its own rather than a silent gap. **R1's first version was a bad probe, not a finding.** Guarding the arm with `if self.worktree_form.is_some()` stops `Msg::Opened` from ever running, so it broke the whole wizard: 65 tests, only one of them the structural guard. A probe has to move the property under test **and nothing else**; the same shape cost T061 a round. **And R3's first version had a stale needle** — `cargo fmt` had wrapped the arm across three lines after the needle was written, so it reported SETUP-FAILED rather than pretending to pass, which is the check working
- [X] T065 [US4] **Extend**, not introduce — the enum exists as of T045, which needed `ClipboardWrite` in Phase 5 (recorded there), **and the draining interpreter exists as of T060**, which needed something to test (recorded there). What remains here is the three variants and wiring them into `app::drain`. Add the remaining three variants to the `Outcome` enum in `crates/micold-client/src/features/mod.rs`, which today declares the first of the four known variants — `SessionsClosed`, `OverlayDismissed`, `ClipboardWrite`, `NotificationRaised` — and the root's draining interpreter with a fixed iteration bound (FR-021, FR-022, FR-024) — **landed at `23e77b8`.** `SessionsClosed(Vec<SessionId>)`, `OverlayDismissed(SurfaceId)` and `NotificationRaised(Notification)` complete the contract's known-outcomes table, and `app::interpret` is the root's answer to them. **It is three lines because the work belongs elsewhere by construction**: a new `session::closed` drops the records, a new `registry::dismiss` closes a surface by id, `notify.push` already existed. That split *is* O3 — the delete path will know *that* its sessions are gone and never *how* a session is dropped. **`interpret` returns `Vec<Outcome>` though none of the three cascades today**, so that "none of these cascade" is a fact about the current interpretations rather than a shape the vocabulary cannot express (FR-024). **Both interpreters list the other's variants rather than using `_`**: effect requests (FR-015a) go to the shell, cross-feature consequences (FR-021) to the root, so a new variant is a compile error in *both* files — which is where the decision about who owns it has to be made. **Two guards fired during the work, both correctly.** `outcome_termination.rs` destructured with an irrefutable `let`, valid only while the enum had one variant — T060's own header predicted this and named T065 as the task that would break it; it now matches, keeping `ClipboardWrite` as its stand-in because a test of the *loop* wants the cheapest payload, not a realistic one. And T059's scan reported `active_session.is_some_and` unclassified, the check that fails rather than guessing; classified as a reader. **The probes found something about this task rather than about the code, and it is the main result: nothing T065 adds is reachable yet.** S1 made `interpret` cascade forever and fired nothing; S2 removed the current-session pointer clearing from `session::closed` and fired nothing. Both for one reason — **`app::drain` and `app::interpret` have no production caller at all**, and neither does `session::closed`. No feature reducer returns `Vec<Outcome>` yet, so there is nothing to drain; T066 is the first emitter and the task list orders it after this one. So the unreachability is correct by sequence rather than an omission, **but it means T065 cannot be probed behaviourally at all**, and the phrase "wiring them into `app::drain`" in this task's own text overstates what can exist before T066. **T066 must add the call site**, not just the emitter. S3 is the one reachable piece and it fires: giving `shell::clipboard::interpret` an `if !text.is_empty()` guard fires `the_shells_translation_decides_nothing` alone — the C3 guard that reads that function's body. Its first version had a stale needle after `cargo fmt` rewrapped the arm, and reported SETUP-FAILED rather than pretending to pass. 104 client suites green, workspace clippy `-D warnings` clean
- [X] T066 [US4] Convert the worktree-delete path to mutate only worktree data and return `SessionsClosed` + `OverlayDismissed`, and confirm `crates/micold-client/tests/worktree_delete.rs` passes **unmodified** (FR-023). **Also add the call site**: T065's probes established that `app::drain` and `app::interpret` have no production caller, because nothing emits outcomes yet — this is the task that gives them one, and until it lands the interpreter is unreachable and unprobeable — **landed at `5503841`. The stated target did not exist, and that is the first finding.** The contract's named anti-pattern — the delete path reaching into session and overlay state — describes code **feature 010** removed in `71c34f8`, before 021 began. Evidence: `worktree::delete_confirmed` writes only `worktree_delete_target`; the shell sends `WorktreeDelete { stop_sessions: true }` and the *daemon* closes the sessions; `State::sessions_in_worktree` has **no production caller at all**; and T059's guard — written to find exactly this shape — names four `worktree` writes, none of them session or overlay state from the delete path. `contracts/feature-outcomes.md` is corrected rather than left to send the next reader the same way. **The named arbiter does not hold either**: `tests/worktree_delete.rs` says it "mirrors the binary's confirmed-delete flow" and hand-rolls `for id in state.sessions_in_worktree(..)` — it mirrors a flow the binary no longer has, so passing it unchanged proves nothing about the delete path. Left frozen under FR-027 rather than edited; what it now asserts deserves its own task. **What landed instead, on the user's call: the first genuine conversion.** `State::set_worktrees` pruned `expanded` — the sidebar's — while pruning four worktree-owned things. It now prunes only its own and returns `Outcome::WorktreesReplaced(names)`; `sidebar::worktrees_replaced` does the rest. **`#[must_use]` on the return value is what makes a caller that forgets to drain a compile error** rather than a silently un-pruned sidebar — it caught 14 test sites and all four shell ones immediately. **Five call sites drain**: the root's `WorktreesLoaded` arm plus startup, two in `workspace`, one in `daemon_sync`. **This is the first time `app::drain` and `app::interpret` are reachable from a live path** — T065's probes established they were not. `ALLOWED` 43 → 42. 104 client suites green, workspace clippy `-D warnings` clean. **Three probes; two of them nested rather than disjoint, and the nesting is the informative part.** (U1) The root stops draining → `reloading_worktrees_drops_stale_expansion_state` alone. (U2) The sidebar's handler stops pruning → that test **plus** `view_state_does_not_carry_from_the_project_you_left`. U1's set is a strict subset of U2's, because U1 breaks only the root's arm while U2 breaks the handler every site shares — **which is the evidence that the four shell call sites are live and covered**, not merely present. (U3) `set_worktrees` goes back to pruning the sidebar itself → `no_feature_writes_another_features_data`, the guard re-catching the write the instant it returns, which is what proves the allowlist deletion was real rather than a line removed from a list
- [X] T067 [US4] **Discovery**: run T059's guard against the full codebase, enumerate every cross-feature write it names, and record the list in `specs/021-mvu-slice-architecture/cross-feature-writes.md` with a proposed outcome variant for each. This task is complete when the list exists — it converts nothing — **`specs/021-mvu-slice-architecture/cross-feature-writes.md` exists; it converts nothing, which is what this task asked.** 42 writes across 27 operations (43 minus the one T066 converted). **The catalogue is organised by cause, not by row, and that is the finding.** Most of these are not a feature reaching into a neighbour at all — they are a *root helper* writing across features, attributed by the guard to whichever feature called it. `State::clear_for_dialog` alone accounts for 8 of the 42. **So T067a is 7 conversions, not 42**: `clear_for_dialog` (8), `focus_terminal` (5), the popover mutual-exclusion rule (12), `Workspace::forget` (4), `push_notification` (2), the reveal (4), and worktree_form-creates-worktree-owns (4), plus three singletons. Converting them one row at a time would mean 42 commits re-deriving the same decision seven times. **An order is proposed with reasons**: `push_notification` first because `NotificationRaised` already exists (T065), so it is a pure call-site change and the cheapest proof the pipeline works; the popover rule next because it is the largest group and `overlay::registry::close_popovers` already implements the mechanism — those arms simply predate it. **Two conversions are blocked on a question rather than on work, and the catalogue says so rather than guessing**: T059's open "is `focus_terminal` a session operation in the wrong file?" governs group B, and the same shape governs `session::switch_active` → `workspace.active` (switching the active project *is* a project operation). Converting a misplaced function into an outcome would enshrine the misplacement, so T067a must settle placement first. **One conversion carries a trap worth flagging early**: the reveal group touches `set_current_session`, and `tests/current_session_writers.rs` holds that every writer of `active_session` goes through it — converting F must not give a second path a way in
- [x] T067a [US4] **Conversion**: for each write enumerated by T067, convert it to return an outcome. **Expanded below, and the expansion is by *cause* rather than by row**: T067 found that most of the 42 are a root helper writing across features, attributed to whichever feature called it, so one commit per row would re-derive the same decision seven times. Seven groups plus three singletons. Ordered so that the cheapest proof comes first and the two blocked on a question come last (FR-020, FR-021). **All ten closed; `ALLOWED` 43 → 0.** The two that were blocked were both answered by *not* writing the outcome the catalogue proposed — see the checkpoint below
  - [x] T067a-1 **E: `push_notification` (2 writes)** — `project::open_refused`, `session::arm_notice`. `NotificationRaised` already exists (T065), so this is a pure call-site change and the cheapest proof the pipeline works. `open_refused` is self-contained; `arm_notice` sits behind `switch_active` → `restore_after_activation` → `arm_notice` and two shell call sites, so it is its own commit. **Both halves done**: `open_refused` in T067a-1 itself, `arm_notice` in T067a-9 once `restore_after_activation` already returned outcomes and the conversion was a one-line change
  - [x] T067a-2 **C: the popover rule — declared in the registry, not assigned in five reducers (12 writes)** — `help::menu_toggled`, `project::menu_toggled`, `project::switcher_toggled`, `sidebar::filter_menu_toggled`, `worktree::menu_toggled`. **Done. `ALLOWED` 12 → 0.** The earlier attempt stopped because the rule is not uniform, and it is worse than "two rules": the three panel popovers are mutually exclusive *and* each also closes the project row menu; the row menu closes two of the three panels and the worktree menu but deliberately **not** the switcher it was right-clicked in; the worktree menu closes only the row menu; the session and terminal menus close nothing at all. No rule expressible in a band or a surface kind is right about all five. So the relation is **declared per surface** rather than derived: a toggle reports `Outcome::SurfaceOpened(id)` and stops, and `overlay::registry::displace` closes each displaced surface through the cancellation that surface already declared. 204 suites / 1939 tests / clippy `-D warnings` green.
    - **The declaration cannot live on the surface, and a guard said so before I did.** The first implementation put `displaces()` on `FloatingSurface`, which made five feature modules name other features' surface types — `tests/surface_registration_cost.rs::a_surface_is_named_only_where_it_lives_and_where_it_registers` fired with ten rows. That test *is* SC-001's operational form: a surface only its own module and the registry can name is a surface nothing else can be special-cased on. It was right. **A relation between two surfaces belongs to neither of them**, and the registry is already the one place every surface is named, so the clause went onto the registration line — `{ displaces: … }` beside `=> view`, which is there for the same reason (a fact the surface cannot state about itself).
    - **The vacuity guard earned its keep, again.** Putting other surfaces' type names inside the `register!` invocation broke the parser in `surface_registration_cost.rs`, which counted each displaced name as a second registration; `the_registration_list_is_actually_being_read` caught it. That is the second time this phase a vacuity floor has paid, and this one paid *while the change that would have hidden behind it was being written* rather than months later. The parser now tracks brace depth and only reads the top level.
    - **Probes.** C1 (the help menu stops declaring it displaces the project row menu) → `the_whole_displacement_relation` and `switcher_forget_menu::opening_any_other_popover_closes_the_project_menu`. C2 (the switcher exception removed) → `the_whole_displacement_relation` and `right_click_opens_the_menu_and_the_switcher_stays_open_behind_it` — a **different** second test from C1's, which is what the exception being a declared fact buys. C3 (root stops applying `SurfaceOpened`) → six tests across four suites. C5 (the help menu assigns the sidebar filter's field again) → the guard plus two `sidebar_state` tests. **C4 fired nothing at first, and the code explained it rather than it being a gap**: making `surface_opened` report an opening unconditionally changes nothing observable, because `displace` resolves the displacing surface out of the set that is *open* and skips a closed one. My comment had claimed the check was what stopped a popover the user never touched from closing; corrected in place. The property is still worth holding one level up — an outcome is a feature's statement of what happened, and the asymmetry rests on those statements being true — so `a_toggle_that_shut_its_surface_reports_nothing` reads the return value, the only place the difference exists, and C4 rerun fires exactly it.
    - **A behaviour change, deliberate and now tested.** The old assignments cleared the neighbours on the way *out* as well as in, so shutting the help menu also shut whatever else was open. Nothing tested it either way; `toggling_a_popover_shut_displaces_nothing` does now.
    - **Thirty of the forty-two ordered pairs had no test.** `tests/popover_displacement.rs` states the whole relation independently and drives every pair through `State::update`, including "the session and terminal menus close nothing", which had never been written down.
  - [x] T067a-3 **D: `Workspace::forget` and `Workspace::activate` (5 writes) — the guard's model changed, not the code.** One call in `micold-core` reaching four members across three features, plus `switch_active` -> `workspace.active` through `Workspace::activate`, which T067a-9 established is the same question. **The placement check said not to convert, and it was right**: `Workspace` is a core *type* and those members are its own, so `forget` is its invariant-preserving operation — everything keyed by this path goes, including the `foreground_by_project` clause whose comment argues the invariant outright. Converting would have three client features each apply one clause of a core invariant, and would make a half-applied forget expressible for the first time. What was wrong was `OWNERS` mapping six `workspace.*` members to three features: right for an ordinary write, and an artefact of a path-keyed map rather than a boundary running through the middle of a core type. **A write a feature reaches *only* through a core method is now exempt**, the same way a feature writing its own field always was. **ALLOWED 17 -> 12**, and the twelve left are all group C.
    - **The scan keeps two closures over one call graph.** `writes` is everything (unchanged for every other consumer); `core` is what a `Workspace` method wrote on the operation's behalf. The exemption applies only to the difference — a path also written on the operation's own line is reported exactly as before, which is what stops the exemption from becoming the third way this guard goes quiet.
    - **Inventoried, not invisible.** The guard has gone silently blind twice (T067a-6, and the `mut_param` floor before it), so an exemption that reported nothing would have been the third. `CORE_MEDIATED` names all five with the method that carries each, `core_mediated_writes_are_inventoried` fails on an unlisted one *and* on one that stops happening, and `the_exemption_is_narrow` holds that every listed path really is owned by another feature. Reaching a neighbour through some other core method now costs a line and an argument rather than nothing.
    - **Probes.** D1 (`forget_confirmed` also writes `workspace.sessions` on a line of its own) → `no_feature_writes_another_features_data` **and** `core_mediated_writes_are_inventoried`, the second because that row stops being mediated — the narrowness demonstrated rather than asserted. D2 (the worktree feature reaches `workspace.active` through `Workspace::activate`, unlisted) → the inventory alone. D3 (`OWNERS` re-owns `workspace.sessions` to `project`) → `the_exemption_is_narrow`, the inventory, and `no_feature_writes`. **Three distinct sets**, which the single-assertion caveat from T067a-4 does not apply to here: this guard now has three assertions that can disagree.

  - [x] T067a-4 **G: the form creates, the worktree feature owns the list (4 writes)** — `worktree_form::created`, `create_failed`, `opened`. **Done; only one of the four was coupling.** The placement check landed on a *field* this time: `worktree_error` is named for the worktree feature but `crate::ui::worktree_form` is its only render site, so it is the modal's error line. Three comments already in the tree said so, and one is load-bearing — `tests/open_project_git_gate.rs` exists because an assertion on this field passed green for as long as a refusal wrote to it with the modal shut, invisible to users. `OWNERS` had it wrong; moving it to `worktree_form` retired three rows with no outcome. The fourth was real — `worktree_form::created` pushed into `worktree`'s list — and became `Outcome::WorktreeCreated(Worktree)` → new `worktree::created`. No `WorktreeCreateFailed` was needed: the failure only ever wrote the form's own field. `created` and `included` stayed two functions despite identical-looking inserts, because they dedupe by different keys (a create names its directory, a daemon include answers with a path). **ALLOWED 28 → 24**; 203 suites / 1934 tests / clippy `-D warnings` green.
    - **Probes.** P1 (`worktree::loaded` writes the form's error again) → guard fires on `worktree` → `worktree_error`. P2 (`worktree_form::created` pushes to the list again) → guard fires on `worktree_form` → `worktrees`. **Two caveats worth keeping.** P1 and P2 fail the *same test*: the guard is a single assertion and can only ever fail once, so their distinctness lives in the reported violation row, not in the set of failing tests — the "each probe fails a distinct set of tests" rule cannot be applied to a single-assertion guard literally. P3 (root stops routing the list change to the form) **fired nothing** across all 104 client suites: the behaviour was unguarded, and had been before this task too, when the clear sat in `worktree::loaded` with no test either. Closed rather than merely recorded — two tests added in `tests/app_state.rs`, after which P3 fails exactly those two and nothing else
  - [X] T067a-5 **A: `clear_for_dialog` — moved, not converted. Eight rows retired and `DialogOpened` turned out to be unnecessary.** Applying T067a-7's lesson *before* designing an outcome: `clear_for_dialog` was root code in `app.rs`, so the guard could not attribute it and reported all eight callers — `help::about_opened`, `project::forget_requested`, `project::rename_started`, `session::remove_requested`, `settings::opened`, `worktree::delete_requested`, `worktree::rename_started`, `worktree_form::opened`. Moved into `features/window.rs`, which has owned `focused_field` since T063 — and **a feature writing its own field is not a cross-feature write at all**, so the eight rows vanished with nothing replacing them. `ALLOWED` 36 → 28; the catalogue's 14 `focused_field` rows are down to 1. **Second time this shape appeared, and the generalisation is now load-bearing for the rest of the burn-down**: a row count is not a violation count when the writer is root code the guard cannot name. T067's proposed `DialogOpened` variant was designing an outcome for a write no feature performs
  - [x] T067a-6a **The guard was attributing direct writes away** (found while starting F; fixed and committed ahead of the conversion). The scan suppressed a violation when the operation called a sibling writing the same field — right, to avoid reporting the callee's write twice, but it could not tell "inherited via the call" from "**also** written right here". `session::started` writes `expanded` and `default_expanded` on its own lines and then calls `set_current_session`, which writes them too, so both of its own writes were attributed away — and a suppressed row is indistinguishable from no row, so the guard stayed green over three unlisted cross-feature writes. The suppression now applies only when the write is not also direct. Surfaced `session::started` → `expanded`, `session::started` → `default_expanded`, `session::restore_after_activation` → `default_expanded`. **ALLOWED 24 → 27**, catalogued naming T067a-6 since all three sit in F's own reveal/commit conversation. **The retirements this phase already made stand** — they were real conversions, probe-verified — **but every count reported before this was measured against the blind spot.** Probe P4 (restore the over-broad suppression) fires `the_allowlist_names_only_live_violations`, a different test from the P1/P2 pair, so it is genuinely distinct
  - [x] T067a-6 **F: not the reveal — the *commit* of the outgoing one (4 writes)** — `session::set_current_session` (3) and `sidebar::toggle_location` (1). **The catalogue named this backwards; corrected before implementing.** `set_current_session` calls `current_session_location()` *before* assigning `active_session`, so the location it resolves is the **outgoing** session's; and `location_open` derives the current reveal as the union of the user's expansion set with the one revealed row, so the incoming reveal is never written at all. These writes therefore **commit the reveal that is ending** into the user's own set, so the row does not silently close underneath them. `SessionRevealed(SessionId)` would have been wrong in both subject and direction — the vocabulary wants to be about the location being *left*, and `pending_reveal_scroll = true` is about the incoming session, making F two outcomes with different subjects rather than one. **Carries a trap**: `tests/current_session_writers.rs` holds that every writer of `active_session` goes through `set_current_session`, so this must not give a second path a way in. Note also its 8 call sites (7 in features, 1 in the shell) all have to drain or propagate.
    - **Done.** Four outcomes: `LocationOpened(SessionLocation)` (fold a location into the user's open set), `RevealScrollArmed`, `ProjectEntered` (view state that must not follow you across), `RevealSuppressed(bool)`. `switch_active` answers `Option<Vec<Outcome>>`; `None` is still the refusal, and `#[must_use]` found all thirteen call sites across lib, shell and tests. **ALLOWED 27 → 19**; 203 suites / 1934 tests / clippy green.
    - **T067a-8 went with it.** `restore_after_activation`'s `show_agent_worktrees` reset is the same fact as `ProjectEntered`, so it needed no variant of its own — the "do it last so it inherits rather than invents" note paid off.
    - **A test hazard worth keeping.** Converting a write into an outcome moves behaviour *out of* the function, so any test calling it directly rather than through `State::update` silently stops exercising it — and still compiles. I applied `.is_some()` uniformly to `switch_active`'s call sites at first; that was right for `features_session.rs` and `session_isolation.rs` (which assert on no sidebar field, exactly the boundary their headers claim) and wrong for `switch_active.rs`, whose tests assert the moved behaviour by name. `switching_arms_a_scroll_and_clears_a_stale_suppression` caught it. Files asserting moved behaviour got drain helpers mirroring the root instead of weakened assertions.
    - **Probes.** F1 (session writes `default_expanded` directly again) → the guard, 1 test. F2 (root stops applying `LocationOpened`) → 9 tests about the commit and the reveal. F3 (root stops applying `ProjectEntered`) → 2 tests, a set **disjoint** from F2's. F4 (swap the order of the commit and the per-project reset in `restore_after_activation`) → **fired nothing**, and the code explains why rather than it being a coverage gap: that function runs *after* `Workspace::activate`, so `current_session_location` resolves the outgoing session's id against the **incoming** project's `active_sessions()`, fails to find it, and answers `None` — the commit cannot fire on that path at all. My own comment had claimed the order was load-bearing; corrected in place, since a wrong claim in a comment is worse than no claim
  - [X] T067a-7 **B: `focus_terminal` — moved, not converted, and that retired six rows without writing an outcome.** T059 asked whether `focus_terminal` was a session operation sitting in the wrong file. It was: the terminal is the session's pane, `terminal_released` is session-owned, and every caller is a session operation. Moved from `app.rs` into `features/session.rs`; **`ALLOWED` 41 → 36 from a file move**. The mechanism is worth stating because it is the general lesson for the rest of the burn-down: `focus_terminal` was *root* code, so the guard could not attribute it and reported all six **callers** instead — `mode_toggled`, `selected`, `shell_instance_selected`, `shell_instance_close_requested`, `started`, `restore_after_activation`. Converting first, as the T067 catalogue originally sequenced, would have given six session reducers an outcome apiece for a write **none of them performs**. One function writes it; one row names it now. **`switch_active` → `workspace.active` is left open and is a different shape**: it is not a misplaced function but a *sequence across two features* — record foreground (session), activate (project), restore (session) — where steps 3–4 depend on step 2 having succeeded. An outcome is applied after the reducer returns, so converting it would break that ordering. It reads as root **composition** under FR-002 rather than as a feature reaching across, but that is a claim about where orchestration belongs and it should be argued on its own, not decided inside a burn-down step. 104 client suites green, workspace clippy `-D warnings` clean
  - [x] T067a-8 **The last singleton** — **absorbed into `ProjectEntered` by T067a-6**, exactly as this entry's own "do it last so it inherits rather than invents" instruction anticipated. No separate change.
  - [x] T067a-9 **The two unblocked singles** — `session::arm_notice` → `notify` becomes `NotificationRaised` (the variant the contract already named and T065 already built); `session::focus_terminal` → `focused_field` becomes a new `FieldFocusCleared`, unconditional unlike `window::field_focus_changed`'s guarded blur, because a press on the pane is a request for *that* terminal and must not be defeated by whichever field still believes it holds the keyboard. `focus_terminal` could not move the way T067a-5/-7 moved theirs — it writes `terminal_released`, which is the session's, and T067a-7 already settled it belongs there — so this one is a real conversion. Cascaded through six session reducers, all found by `#[must_use]`. **ALLOWED 19 → 17.**
    - **The third single is not a single.** `session::switch_active` → `workspace.active` goes through `Workspace::activate`, which makes it **the same question as group D** — a core `Workspace` method called from a feature — rather than an independent row. Left for that decision instead of converted.
    - **Fourth instance of F's test hazard, in a fourth file.** `switch_active.rs`, `background_restart.rs` and `daemon_sync.rs`'s own unit test each assert a consequence that used to happen inside the function. **`#[must_use]` does not catch these**: a test that consumes the value into an `assert!` and never applies it satisfies the attribute. Only running them found it. Each got a drain helper mirroring the root.
    - **Probes.** S1 (session writes `notify` directly) → guard, row `arm_notice — writes state.notify, owned by notifications`. S2 (session writes `focused_field` directly) → guard, **different row**: `focus_terminal — writes state.focused_field, owned by window`. S3 (root stops applying `FieldFocusCleared`) → `pressing_the_pane_wins_over_a_field_that_held_the_keyboard`. S4 (root stops applying `NotificationRaised`) → 4 tests, a set **disjoint** from S3's.
  - [x] ~~T067a-8 (superseded)~~ — `session::restore_after_activation` → `show_agent_worktrees` (feature 014, FR-010e). Absorbed into `Outcome::ProjectEntered` by T067a-6, exactly as its own "do it last so it inherits rather than invents" instruction anticipated. T067a-3 settled the other half by not converting it at all
- [x] T068 [US2] Write the "read/write asymmetry" section of `docs/development/architecture.md`, including why guard tests hold the line rather than the type system (plan.md Complexity Tracking). **Written from what this phase actually cost** rather than from the plan: the asymmetry itself (a read cannot leave the app in a state its owner did not choose; a write can), why the allowlist is what a guard test buys over splitting `State`, `#[must_use]` on outcome returns, and three traps — a row count is not a violation count when the writer is code the guard cannot name; ownership can be wrong and a plausible name is how it stays wrong; a test calling a feature function directly stops exercising what moved, and `#[must_use]` does not catch that. Closes with the vacuity guard and the reason it exists: this guard has gone silently blind twice, so **a green guard is evidence only in proportion to what you have shown it can still fail on**

**Checkpoint**: Zero direct cross-feature reducer writes. Root is routing only. 22 variants left the
root message enum.

> **Met.** Root *is* routing only (`ROOT_DECISION_ARMS` 93 → 0, T063), the message-enum reduction
> landed, and `ALLOWED` is empty: **43 → 0**. The two groups that were blocked on a decision wider
> than a burn-down step were both decided against the shape T067 proposed for them. Group D wanted
> no conversion at all — `Workspace::forget` is core's own invariant, and it was the *guard's* model
> of ownership that was wrong (T067a-3). Group C wanted the rule moved rather than an outcome per
> write, and moved to the registry rather than onto the surfaces, because a relation between two
> surfaces belongs to neither (T067a-2).
>
> **Four of the seven groups did not want the outcome the catalogue proposed**, and the pattern is
> the same each time: ask where the code belongs before designing a message for it. Two were
> misplaced functions (A, B — fourteen rows, no outcome), one was a mis-owned field (G — three
> rows), one was a mis-modelled type (D — five rows), and one was a relation with no owner (C —
> twelve rows). Only four rows in the whole phase genuinely needed an outcome written for them.

---

## Phase 7: Polish & Cross-Cutting Concerns

- [x] T069 Measure SC-003: run `find crates -name '*.rs' -exec wc -l {} + | sort -rn | head -10` and confirm neither `app.rs` nor `main.rs` is near the top and neither holds more than one feature. **FR-005 governs; the 500-line figure is indicative** (clarified 2026-08-07). Record the count as a progress signal — do NOT split a single-feature module to cross a threshold
  - **Half met, and the half that is not is `main.rs`.** Measured against the branch point `e02f971`:

    | file | then | now | rank then | rank now |
    |---|---|---|---|---|
    | `src/app.rs` | 1885 | **1334** | 2nd | **8th** |
    | `src/main.rs` | 1672 | **1675** | 5th | **4th** |
    | `src/features/` (total) | 1938 (10 files) | **3426** (11 files) | — | — |

    `app.rs` shed 551 lines and five places. `main.rs` did not move — it is *three lines longer* — and it ranks higher now only because `app.rs` fell past it. **So "neither is near the top" is false for `main.rs`, and saying otherwise would be reading the ranking backwards.**
  - **The substantive half is met, and permanently.** Neither holds more than one feature, and that is not a measurement any more: `tests/root_is_routing_only.rs` pins `ROOT_DECISION_ARMS` at an exact **0** (T063), so a new feature adding one decision to the root fails the day it lands. What is left in `app.rs` is two shared vocabularies and a routing table — the `Message` enum is 408 lines and the `State` struct 188, together 596 of the 1334; `update` is 285 lines of one arm per message; and 664 lines (half the file) are comments. Its line count measures the size of the application's vocabulary, not the root's logic, which is why **no split is proposed** — the task's own instruction, and the right call here.
  - **`main.rs` is the iced adaptation layer and is out of FR-005's scope by design.** FR-006 keeps rendering out of feature modules, so the binary is where the framework is named; its 1176 code lines are `view`/`update_inner`/subscriptions/effects. It was never a target of this feature and did not become one. **Recorded as a standing signal rather than closed**: the shell's `update_inner` is unreachable from integration tests (flagged during T067a), which is the fact that would matter if it were ever split.

- [ ] T070 [P] Add the **permanent** SC-002 guard in `crates/micold-client/tests/feature_registration_cost.rs`, failing if adding a feature would require edits beyond its own module and one registration point (SC-002, SC-002a)
- [x] T071 [P] Verify SC-004 — each of the **eight** feature modules has an isolation test (T007–T014). The overlay registry is a ninth module but not a feature module, and is covered by its own guards instead
  - **There are ten feature modules, not eight, and two had no isolation test.** The eight named by SC-004 all have one. `features/help.rs` (created by T031 for the homeless overflow menu) and `features/window.rs` (created by T063 for three fields the root was still deciding about) were both created *after* the list of eight was drawn up, and neither got a file. **That is the failure mode SC-004 shares with SC-001**: a criterion phrased as a count is satisfied by the day it was written and says nothing about the day after.
  - **Closed rather than recorded.** `tests/features_help.rs` (5 tests) and `tests/features_window.rs` (5 tests) now exist, written to the boundary the existing files actually hold — build a `State` if the reducers need one, but name no other feature's types. `features_window.rs` pins the focus asymmetry that BUG-003 turned on: a *blur* is believed only from the field that holds focus, while `field_focus_cleared` is unconditional, which is why they are two functions.
  - **One assumption of mine was wrong, and the test that caught it stayed.** I wrote a case asserting `help::about_opened` leaves the menu open, on the reasoning that it writes one field. It calls `State::clear_for_dialog` first, so choosing "About" takes the menu down with it (FR-012) — correct, expected by any user, and invisible from inside the module. The test now asserts what actually happens, and says in its own comment that it was written backwards first.
  - **The count is made permanent by T070**, which adds the guard that every module under `src/features/` has a `features_<name>.rs`. Verifying a number by hand is what let this drift twice.

- [ ] T072 Perform quickstart.md procedure M1 (persisted state written by the pre-change build loads and behaves identically) and record the result (SC-008, FR-026)
- [ ] T073 Run the FR-027 check across the whole feature branch: `scripts/check-assertions-frozen.sh main` must report nothing but pure relocations *(recipe corrected 2026-08-11 — this task used to prescribe `git diff main...HEAD -- crates/*/tests/ | grep -E '^-.*assert'`, which issue #146 showed is unsound: an assertion edited in place leaves its `assert!(` opener as diff context on neither side, so a reversed expectation matches nothing and passes. Use the script, which extracts both sides from whole files. It also covers `crates/*/src/`, which the grep recipe never did — the inline test modules T073 exists to police live there)*
- [ ] T074 [P] Promote T004's assertion-freeze job from advisory to blocking in `.github/workflows/ci.yml` now its false-positive rate is known. **Unblocked 2026-08-11 — scoping landed; the promotion itself is still open.** The job ran on every non-docs-only change on every branch while FR-027 binds only changes made for this feature, so blocking it would have stopped every concurrent feature from changing any expectation it owns (feature 023 legitimately changed twelve, and the job reported all twelve). The script now decides scope from the change itself — it touches `specs/021-mvu-slice-architecture/`, or its branch names 021 — and out of scope it prints the identical report and exits 0, so a change is still told what it did to the suite without being blocked (spec.md Q3; `ASSERTION_FREEZE=enforce|report` overrides; `scripts/tests/check-assertions-frozen.test.sh` pins all of it). What remains for this task is the original judgement and nothing more: whether the false-positive rate *within* 021 is low enough to remove `continue-on-error`
- [ ] T075 [P] Review `docs/development/architecture.md` end to end for coherence and update `docs/README.md` navigation
- [ ] T076 Confirm SC-009 from history: every task above is its own commit, and each commit builds, runs and passes
- [ ] T077 Verify the full suite green on Linux, macOS and Windows (SC-006, Principle VI)
- [ ] T078 Re-measure the baseline table in `specs/021-mvu-slice-architecture/spec.md` against the final tree and record the delta — the figures have moved four times during this feature's life

---

## Dependencies & Execution Order

### Phase Dependencies

- **Phase 1 (Setup)**: No dependencies
- **Phase 2 (Foundational)**: Depends on Phase 1. **Blocks everything** — T004 is what makes FR-027 enforceable
- **Phase 3 (US2 part 1, Tier 1)**: Depends on Phase 2. The MVP
- **Phase 4 (US1, Tier 2)**: Depends on Phase 3 — the registry needs feature modules to register into (spec, US2's "Why this priority")
- **Phase 5 (US3, shell)**: Depends on Phase 2 only. **Orthogonal to Tiers 1–3** (FR-019a) — can run parallel to Phases 3–4 with a second developer
- **Phase 6 (US2 part 2 + US4, Tier 3)**: Depends on Phases 3, 4 and 5. Last, because §5's nesting evidence is only trustworthy once boundaries are visible
- **Phase 7 (Polish)**: Depends on all above

### Critical constraint

**Phase 5 must complete and be recorded green (T058) before Phase 6 begins.** SC-004b requires
demonstrating Tiers 1, 2 and the shell split with zero Tier 3 merged. Starting Phase 6 early makes
that criterion unverifiable — it is the one ordering rule that cannot be relaxed for convenience.

### Within each phase

- Tests written and observed failing before implementation (Principle I)
- Extraction tasks are sequential where they touch the same file (all of Phase 3 edits `app.rs`)
- Architectural docs ship inside their own phase, not deferred (Principle VII)

### Parallel Opportunities

- T002, T003 in Setup
- T005, T006 in Foundational
- All eight isolation tests T007–T014 plus the render-free guard T014a (separate files)
- All five capability tests T041–T045 (separate files)
- T059–T061 (separate files)
- **Phase 5 against Phases 3–4** — the shell split is orthogonal by FR-019a, the single largest parallelization win here
- T070, T071, T074, T075 in Polish

**Not parallelizable**: T015–T023 all edit `app.rs`. T028–T037 form a deliberate serial chain so a
bisect lands on one overlay change.

---

## Parallel Example: Phase 3 tests

```bash
# All eight isolation tests are separate files with no shared state.
# Each fails to compile until its module exists — that is the Red state.
# T014a joins them: a regression lock that passes from the start and must keep passing.
Task: "Isolation test for the worktree-creation form in crates/micold-client/tests/features_worktree_form.rs"
Task: "Isolation test for sidebar types in crates/micold-client/tests/features_sidebar.rs"
Task: "Isolation test for project/workspace types in crates/micold-client/tests/features_project.rs"
Task: "Isolation test for settings types in crates/micold-client/tests/features_settings.rs"
Task: "Isolation test for worktree types in crates/micold-client/tests/features_worktree.rs"
Task: "Isolation test for notification types in crates/micold-client/tests/features_notifications.rs"
Task: "Isolation test for session types in crates/micold-client/tests/features_session.rs"
Task: "Isolation test for daemon-connection types in crates/micold-client/tests/features_connection.rs"
```

---

## Implementation Strategy

### MVP (Phase 3 only)

1. Phase 1 Setup → Phase 2 Foundational
2. Phase 3 — Tier 1 feature modules
3. **STOP and VALIDATE**: every feature answers "where does it live?" with one module (SC-010);
   `app.rs` roughly 1,700 lines; suite green on three platforms
4. This is a complete, shippable improvement. Tiers 2 and 3 need not follow immediately.

### Incremental Delivery

Each phase is independently shippable (FR-004c), which is unusual and worth exploiting:

1. Setup + Foundational → safety nets in place
2. Phase 3 → feature modules → **ship** (MVP)
3. Phase 4 → overlay registry → **ship** (removes the largest source of "I forgot a site" bugs)
4. Phase 5 → capabilities and shell split → **ship** (SC-004b checkpoint)
5. Phase 6 → reducer split and outcomes → **ship**
6. Phase 7 → verification and measurement

### Parallel Team Strategy

Two developers, after Phase 2:

- Developer A: Phase 3 → Phase 4 (Tiers 1 and 2, both touching `app.rs`)
- Developer B: Phase 5 (shell split, touching `main.rs`)

The split is clean because FR-019a makes the shell orthogonal to the tiers, and the two developers
touch different files. They converge for Phase 6, which needs both.

---

## Notes

- **Every task is its own commit.** SC-009 is verified from `git log`, not from the endpoint
- Verify tests fail before implementing — for extraction, the existing suite already provides Red
- The existing 71-file suite is frozen (FR-027). Additions and relocations are fine; rewrites and
  deletions are defects
- Re-measure rather than trusting any figure here — the baseline has moved four times in ten days
- **T022 covers a gap**: research.md §6's Tier 1 table lists seven steps for eight features. The
  daemon-connection extraction was missing and is added here
