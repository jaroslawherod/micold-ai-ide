# Autopilot ledger — 027-sandboxed-daemon-runtime BUG-005

Maintained by the `speckit-autopilot` skill. It is the record of what this flow owns and how far it
has got. `resume` finds this file by its **Worktree branch** line and reads it. Keep it true.

- **Input**: rebase main and then run speckit-autopilot skill
- **Kind**: bug
- **Worktree branch**: fix/the-container-daemon-is-not-automatically-started
- **Started**: 2026-09-14
- **Phase**: 4-milestones
- **Next step**: wait for `ci complete` on #359 to go green (D5), rebasing onto main as 030 lands fixes; then `gh pr merge 359 --rebase` and the handoff

## Pull requests

| PR | Purpose | Status | Merge SHA |
|---|---|---|---|
| #359 | BUG-005 fix (record, spec patch, regression tests, fix) | open | — |

## Milestones

| ID | Tasks | Deliverable | PR | Status |
|---|---|---|---|---|
| M1 | T167–T207 | Switching to *In a container* brings the sandbox's service up when it is not running, reports its stages, and brings it back up when it is lost (FR-002a, FR-036a, FR-036b, SC-004c, US6 scenario 9) | #359 | in-review |

## Decisions

| # | Phase | Question | Answer | By | Evidence |
|---|---|---|---|---|---|
| D1 | 0-bug | Where does autopilot enter? The bug was reported, patched, verified and mostly implemented in this worktree before autopilot was invoked | Enter at Phase 0 step 6 (the milestone loop) with the work as it stands. The steps already done are not re-run | agent-resolved | `bugs/BUG-005.md` (Status: Patched 2026-09-12); `tasks.md` Phases 18–24 |
| D2 | 0-bug | The patch added 36 tasks (T167–T202), over step 5's limit of 10, which would switch to Phase 1 | Stay on the bug path. The patch adds no behaviour the spec did not intend: FR-002a's "start its service when absent" and US6's recovery were already specified for every placement, and most of the tasks are TDD remediation from six audits, not scope | agent-resolved | `spec.md` FR-002a, FR-036a/b; `tasks.md` Phases 20–24 are audit remediation |
| D3 | 0-bug | `main` gained its own 027 `bugs/BUG-004.md` (the settings-rail slide, routed to 030) while this bug held the same number locally | This bug is renumbered BUG-005. Main's record keeps BUG-004, and 030's ledger cites it by that number. Commit messages written before the rebase still say BUG-004 | agent-resolved | `origin/main:specs/030-settings-rail-slide/autopilot.md` D5, D6 |
| D5 | 4-milestones | #359 is red only in feature 030's Windows install smoke ("no micold-daemon.exe process was found" after uninstall with the client open) | Wait for 030: keep watching #359's CI, merge when `ci complete` is green, no code change here | user | AskUserQuestion 2026-09-16; run 35128268421 attempts 1–2 |
| D4 | 0-bug | Adding `iced_runtime` as a `micold-client` dev-dependency (T201) | Approved | user | AskUserQuestion 2026-09-14 |

## Declined review findings

| Milestone | Review | Finding | Why declined |
|---|---|---|---|
| M1 | A (round 2) | A1 / B F1: cancelling does not stop a runtime call under way; the container can be created after the move's stop | Not new with BUG-005: the launch-time bring-up has always raced `stop_old` the same way. Stopping it needs a cancel flag threaded through core's `start` and a stop after it, beyond a MINOR fix. The comment now says so; recorded under follow-ups |
| M1 | A (round 2) | A3: a refusal (e.g. `StaleDevImage`) during the start grace is absorbed once | Bounded to one reconnect backoff (`REFUSALS_WHILE_STARTING = 1`), and the same refusal is reported on the next dial. Telling a refusal from an absence needs a new `ConnectionMsg` variant in feature 028's connection contract; recorded under follow-ups |
| M1 | B (round 1) | F3: re-run T181's real-runtime suite before merge | Not declined outright: rebuilding the shared `micold-daemon:dev` image needs approval and collides with other worktrees. The PR body states T181 was not re-run after Phases 20–25 (kept under follow-ups) |

## Review rounds

| Milestone | Round | A | B | Resolution |
|---|---|---|---|---|
| M1 | 2 | 3 LOW: cancel leaves blocking `start` running (same as B F1); stale "Trying again" after Restart; first refusal from an answering daemon absorbed during start grace | CLEAN, 3 MINOR: F1 cancel comment false; F2 branch conflicts with main's `main_tests.rs` move; F3 fallback cancel untested | Rebased (F2); T207 (A2); comments corrected (A1/F1, F3); A3 declined, see below |
| M1 | 1 | 3 findings: budget refilled on `Started`; stale `Progress` after a host move; waiting bring-up never cancelled | CHANGES: F1 (guide claims cancellation; same as A3), F2 `LIVE` token, F3 T181 | T203–T206 (Phase 25) |

## Open escalation

None.

## Follow-ups not done

- Feature 030's `scripts/windows-install-smoke.sh` step 9a races the client's reconnect (investigated
  2026-09-16 for #359). `InitializeUninstall` runs `StopDaemon` *before* the `AppMutex` refusal, so the
  refused uninstall has already killed the daemon; `find_daemon_pid` then runs ~0.6–1.2s later, while
  the open client only respawns one `RECONNECT_BACKOFF` (1s) after the pipe closes. #359 run
  35131568723: daemon started 18:05:17.39, uninstall log 18:05:18.04, FAIL 18:05:19.27, no new daemon in
  `micold-daemon.log` by its 18:05:19.33 dump. Main's pass at `9a6a7738` found a daemon ~1.3s after the
  refusal. #359 changes nothing on the host placement's connect/spawn path.

- A bring-up cancelled while its blocking `start` is under way (e.g. mid image pull) can still create
  `micold-sandbox` after the placement move's stop found none; the launch-time bring-up has the same
  race (review A round 2, finding 1).
- During the start grace (`awaiting_service`), a refusal from a daemon that answered is absorbed like
  an absence for one backoff; `ConnectFailed` does not distinguish the two (review A round 2, finding 3).

- Two older client save tests write the developer's real `settings.json` on every test run
  (`crates/micold-client/src/main.rs`, the tests through `base_app()`); predates this bug
  (`tdd/verification.md` finding 2).
- T181 (real-runtime lifecycle against `micold-daemon:dev`) was not re-run after Phases 20–25;
  rebuilding the shared image needs approval.
