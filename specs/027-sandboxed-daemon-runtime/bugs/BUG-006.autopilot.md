# Autopilot ledger — 027-sandboxed-daemon-runtime [BUG-006]

Maintained by the `speckit-autopilot` skill. It is the record of what this flow owns and how far it
has got. `resume` finds this file by its **Worktree branch** line and reads it. Keep it true.

- **Input**: running in sandbox, can't store sessions
- **Kind**: bug
- **Worktree branch**: fix/running-in-sandbox-can-t-store-sessions
- **Started**: 2026-09-18
- **Phase**: 4-milestones
- **Next step**: run `mise run gate`, then reviews A and B, then open the fix PR

The report, patch, verification and implementation were done in earlier sessions without this
ledger; it was adopted on 2026-09-22 with the work already committed. The state below is
reconstructed from the commits, the BUG record and `specs/027-sandboxed-daemon-runtime/tasks.md`.

## Pull requests

| PR | Purpose | Status | Merge SHA |
|---|---|---|---|
| #… | Fix (BUG-006) | not opened yet | … |

## Milestones

| ID | Tasks | Deliverable | PR | Status |
|---|---|---|---|---|
| M1 | T208–T215 | A `claude` session in the sandbox keeps its conversation across a restart, with the host sign-in shared and without it | #… | in-review |

## Decisions

| # | Phase | Question | Answer | By | Evidence |
|---|---|---|---|---|---|
| D1 | bug | Share the whole `~/.claude`, or only the sign-in? | Only `.credentials.json`, mounted writable so `claude` can refresh the token; every other credential stays read-only | agent-resolved | `bugs/BUG-006.md#mechanism`, T211 |
| D2 | bug | Where does a sandboxed session's transcript live? | In the sandbox's own home, `<state>/sandbox-home/.claude`; the boot prune leaves sandboxed sessions to the daemon | agent-resolved | `bugs/BUG-006.md`, T210, T212 |
| D3 | bug | Adopt this ledger and ship the committed work, or stop? | Adopt and ship | user | AskUserQuestion 2026-09-22 |

## Declined review findings

| Milestone | Review | Finding | Why declined |
|---|---|---|---|

## Open escalation

None.

## Follow-ups not done

- The first interactive `claude` in a fresh sandbox home runs its own onboarding, which asks for a
  login method although the shared token is valid. Recorded in
  `evidence/bug-006-sign-in-sessions.md`. Out of BUG-006's scope; needs its own bug record.
