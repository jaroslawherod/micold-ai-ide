# Autopilot ledger — 027-sandboxed-daemon-runtime [BUG-006]

Maintained by the `speckit-autopilot` skill. It is the record of what this flow owns and how far it
has got. `resume` finds this file by its **Worktree branch** line and reads it. Keep it true.

- **Input**: running in sandbox, can't store sessions
- **Kind**: bug
- **Worktree branch**: fix/running-in-sandbox-can-t-store-sessions
- **Started**: 2026-09-18
- **Phase**: 4-milestones
- **Next step**: `mise run gate` after the review remediation, then open the fix PR

The report, patch, verification and implementation were done in earlier sessions without this
ledger; it was adopted on 2026-09-22 with the work already committed. The state below is
reconstructed from the commits, the BUG record and `specs/027-sandboxed-daemon-runtime/tasks.md`.

## Pull requests

| PR | Purpose | Status | Merge SHA |
|---|---|---|---|
| #398 | Fix (BUG-006) | open | … |

## Milestones

| ID | Tasks | Deliverable | PR | Status |
|---|---|---|---|---|
| M1 | T208–T215 | A `claude` session in the sandbox keeps its conversation across a restart, with the host sign-in shared and without it | #398 | ci |

## Decisions

| # | Phase | Question | Answer | By | Evidence |
|---|---|---|---|---|---|
| D1 | bug | Share the whole `~/.claude`, or only the sign-in? | Only `.credentials.json`, mounted writable so `claude` can refresh the token; every other credential stays read-only | agent-resolved | `bugs/BUG-006.md#mechanism`, T211 |
| D2 | bug | Where does a sandboxed session's transcript live? | In the sandbox's own home, `<state>/sandbox-home/.claude`; the boot prune leaves sandboxed sessions to the daemon | agent-resolved | `bugs/BUG-006.md`, T210, T212 |
| D3 | bug | Adopt this ledger and ship the committed work, or stop? | Adopt and ship | user | AskUserQuestion 2026-09-22 |

## Review findings acted on

Reviews A and B ran from fresh context over `origin/main...HEAD` on 2026-09-22 and found six, each
confirmed against the code before it was acted on. All six are fixed on this branch, test first.

| # | Severity | Finding | What was done | Cycle |
|---|---|---|---|---|
| F1 | high | The placement read at boot called `settings().load()` before the read that reports a recovery, and the real store answers "recovered" only once, so a corrupt `settings.json` reset every preference in silence (BUG-025 again) | One read, in `restore_from_disk`, feeding the placement, the status and the settings | U40 |
| F2 | high | The sign-in share is a file path and nothing checked it was there; a runtime creates a missing bind source as a root-owned directory in the user's real home | `HostFacts::gather` drops an absent token; `CredentialLayout::conventional` stays pure | U41 |
| F3 | medium | Only the sign-in's *directory* in the sandbox home was pre-created, so the runtime created the target file itself, root-owned, and it survived the container | `MountSet::home_files_to_create`, created 0600 by the bring-up, replacing only an unwritable one | U42 |
| F4 | low | The `LocalSandbox` early return in the boot prune also skipped the sweep that forgets a project with no sessions | Only the judging is conditional now | U43 |
| F5 | minor | T208 claimed an assertion no test made | The assertion is made, over every share | U44 |
| F6 | minor | The user guide did not say what moving back to the host does to sandboxed sessions, and Phase 26 had no verify line | Both written | — |

## Declined review findings

| Milestone | Review | Finding | Why declined |
|---|---|---|---|

## Open escalation

None.

## Follow-ups not done

- The first interactive `claude` in a fresh sandbox home runs its own onboarding, which asks for a
  login method although the shared token is valid. Recorded in
  `evidence/bug-006-sign-in-sessions.md`. Out of BUG-006's scope; needs its own bug record.
- A sign-in share that is on while the host has no token file mounts nothing, and says so only in the
  log. The spec's macOS Keychain edge case asks for the absent item to be *reported*, and `observe`
  carries a `SandboxState` and nothing else, so a user-visible report needs a channel this fix
  deliberately did not build (review finding F2). Needs its own increment.
