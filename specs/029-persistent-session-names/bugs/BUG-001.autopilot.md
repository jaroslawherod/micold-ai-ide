# Autopilot ledger — 029-persistent-session-names BUG-001

Maintained by the `speckit-autopilot` skill. It is the record of what this flow owns and how far it
has got. `resume` finds this file by its **Worktree branch** line and reads it. Keep it true.

- **Input**: bug: see the screen show the name of past session is still not displayed even when the bug was reported and fixed
- **Kind**: bug
- **Worktree branch**: fix/the-name-of-past-session-is-still-not-shown
- **Started**: 2026-09-19
- **Phase**: done
- **Next step**: None here. The flow continues as feature 032; its ledger is [specs/032-untitled-session-labels/autopilot.md](../../032-untitled-session-labels/autopilot.md).

## Pull requests

| PR | Purpose | Status | Merge SHA |
|---|---|---|---|

## Milestones

| ID | Tasks | Deliverable | PR | Status |
|---|---|---|---|---|

None. The size decision is to switch to Phase 1 (D2), so no Phase 0 fix milestone exists.

## Decisions

| # | Phase | Question | Answer | By | Evidence |
|---|---|---|---|---|---|
| D1 | 0-bug | Does it reproduce, and which spec owns it? | Yes, on installed 0.15.0 (which contains 029). Unchanged naming code on `origin/main` 8cb88167 (code check below). The four rows' `claude` transcripts have 0 `ai-title` records; the named row's has 52. The owner is 029-persistent-session-names (Closed 2026-09-14, no ledger, so not in flight). | agent-resolved | BUG-001.md §Reproduction; repro test result below |
| D2 | 0-bug | One milestone, or Phase 1? | Phase 1. For these four conversations `claude` never wrote an `ai-title`, although each has typed prompts. When it does write one is not known: 10 of the 80 most recent transcripts have typed prompts but no title. So these sessions have no name anywhere in the CLI's records. Showing one needs a new label source, which contradicts FR-004 and the spec's "single source" assumption. That is new behaviour, and the bug rubric sends it to Phase 1. So `speckit-bugfix-patch` / `-verify` were not run: patching a closed spec with new behaviour is what the rubric forbids. | agent-resolved | SKILL.md Phase 0 step 5; review-rubrics.md §Bug rubric |

### Reproduction on `origin/main` (8cb88167)

Run 2026-09-19. A throwaway test, `crates/micold-core/tests/zz_bug_repro_tmp.rs` (deleted after the run, never committed), copied the real transcripts into a temporary config dir and called `ClaudeProvider` directly:

```
REPRO untitled.jsonl -> None                          # e0912fd9…, transcript has no ai-title
REPRO titled.jsonl -> Some("Merge when CI is green")  # 9a657bb1…
REPRO term-title 'Claude Code' -> None                # name_in_terminal_title
test result: ok. 1 passed
```

## Declined review findings

| Milestone | Review | Finding | Why declined |
|---|---|---|---|

None declined.

### Review log

| Round | Model | Verdict | Findings | Resolution |
|---|---|---|---|---|
| 1 | session (opus) | CHANGES | F1 MAJOR: the "ai-title only after a plain typed prompt" mechanism is false, because all four have typed prompts. F2 MAJOR: the code-check result was stated before the test ran. F3 MINOR: the claim that claude names the session only in `sessions/<pid>.json` ignores `custom-title` and `agent-name`. F4 MINOR: counts and line numbers. | All four verified and fixed. The mechanism is restated as observed and unexplained; the code check was run (below); the custom-title finding is a follow-up; the numbers are corrected. The size decision is unaffected. |
| 2 | sonnet | CLEAN | none | — |

## Open escalation

None.

## Follow-ups not done

- **Candidate 029 BUG (in scope, not this flow)**: `ClaudeProvider::parse_title` (`crates/micold-core/src/provider.rs:337`) reads only `ai-title`. It ignores the transcript's `{"type":"custom-title","customTitle":…}` records, written by the user's `/rename`. Example: `3ef7391c` has custom-title "feature encapsulation implementation" at line 10974, then ai-title "BBBBBBBBBBBBBB" at 10975, so recovery restores the ai-title over the user's rename. Also `agent-name` records exist. This may break FR-005 / FR-006. Found by the round-1 reviewer; not reproduced in the app.
- Unverified: in project `micold_time_track` (`aec172eed489a079.json`), the non-archived sessions `add64c3d…` and `090b1952…` are stored untitled, yet their transcripts hold `ai-title`s ("Investigate reported bugfix", "Speckit upgrade and cleanup"). Recovery runs only when a project is opened (`refresh_worktrees_off_runtime`), and this project has probably not been opened since 0.15.0 was installed. If they still read "New session" after the project is opened, that is a separate 029 FR-006 defect.
