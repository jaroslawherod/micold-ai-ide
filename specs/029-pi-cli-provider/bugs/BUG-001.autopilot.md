# Autopilot ledger — 029-pi-cli-provider BUG-001

Maintained by the `speckit-autopilot` skill. It is the record of what this flow owns and how far it
has got. `resume` finds this file by its **Worktree branch** line and reads it. Keep it true.

- **Input**: I would like to start pi coding session in same way as I start copilot or claude session but I don't see such option
- **Kind**: bug
- **Worktree branch**: fix/ican-t-start-pi-codeing-session
- **Started**: 2026-09-19
- **Phase**: 0-bug
- **Next step**: bug-rubric review round 2 (Sonnet) of the round-1 fixes

## Progress (bug path)

1. Reproduce — done 2026-09-18 on the reporter's installed build (code path unchanged on
   `origin/main`): `pi` only under `~/.local/share/mise/installs/node/24/bin`, daemon `PATH` without
   it, `available_here()` omits Pi. Recorded in `bugs/BUG-001.md`.
2. Owning spec — `specs/029-pi-cli-provider`, Status Closed 2026-09-14.
3. Report — `bugs/BUG-001.md`.
4. Patch + verify — done (FR-003b, SC-001a, SC-006a amended; T019 reopened; T062–T070). Verify:
   all checks pass. TDD plan added T071–T075 and `tdd/test-list.md`.
   Bug-rubric review — round 1 2026-09-19: CHANGES (F1 BLOCKER no repro steps and reporter's
   env-include off; F2 MINOR start_session gate missing from plan/report; F3 MINOR T069 is a separate
   defect; F4 MINOR T019 double marker). All four fixed. Round 2 — pending.
5. Size — stays on the bug path: the fix restores behaviour FR-003 already intended; the patch
   added 9 tasks (≤ 10). T071–T075 are test/closure tasks the TDD plan split out.
6. One milestone — pending.

## Pull requests

| PR | Purpose | Status | Merge SHA |
|---|---|---|---|

## Milestones

| ID | Tasks | Deliverable | PR | Status |
|---|---|---|---|---|
| M1 | T019, T062–T068, T070–T075 (T069 moved out) | A CLI reachable only through the env-include `PATH` (e.g. `pi` under mise/nvm) is offered and starts | — | pending |

## Decisions

| # | Phase | Question | Answer | By | Evidence |
|---|---|---|---|---|---|
| D1 | bug | Which directory's environment decides availability in Settings, where no directory is in play? | The user's home directory | agent-resolved | spec.md FR-003b; `state.rs` `env_include_cache` is per-directory |
| D2 | bug | Must the start-time gate in `start_session` change too? | Yes — otherwise an offered CLI is refused at launch | agent-resolved | `crates/micold-daemon/src/state.rs` `start_session` `!provider.is_available()`; tasks T071/T074 |

## Declined review findings

| Milestone | Review | Finding | Why declined |
|---|---|---|---|

## Open escalation

None.

## Follow-ups not done

- T069: how `/tmp/does-not-exist.sh` reached a real `settings.json` — separate defect, prior
  evidence `specs/010-daemon-session-persistence/bugs/BUG-025.md`, likely writer
  `crates/micold-client/src/main_tests.rs:1587`.
- The reporter's real `settings.json` holds `env_include_enabled: false` with a
  `/tmp/does-not-exist.sh` fixture path (T069 investigates). After the fix they must turn
  env-include back on for Pi to be offered.
